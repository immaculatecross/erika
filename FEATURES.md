# Milestones

Statuses: `backlog` → `next` → `building` → `done`, plus `folded` (subsumed by a later milestone, named in the row). One milestone = one mission handed to the dispatcher. Version scope: **v0.1 = E-1…E-5** (D-5, D-12) · **v0.2 = E-7, E-9, E-11, E-6, E-12** in that build order (D-15) · **v0.3 = E-16…E-23** in ID order (D-16, ratified from RETRO-001) · **the Learn era (D-17, amended by the plan review 2026-07-23; v0.6+v0.7 merged 2026-07-24, operator-directed): v0.4 = E-24…E-29 · v0.5 = E-30…E-34 · **v0.6 = E-35, E-36, E-37, E-38** ("feature-complete") · v0.7 = E-39 · v0.8 = E-40 · v1.0 = E-41 (v0.9 skipped)**, each version in ID order, every prefix a coherent release. E-1…E-15 are listed in original roadmap order; the build order is whatever the version decision names.

| ID | Status | Milestone | Acceptance test |
|---|---|---|---|
| E-1 | done | Foundation — shell & design system | `npm run dev` serves the app; sidebar nav (Sessions, Practice, Settings) with empty states obeying DESIGN.md; page transitions and list-stagger animations present (Motion), `prefers-reduced-motion` respected; SQLite via a migrations runner; Settings persists target language, native language, model tier, and monthly budget across reloads; `lint`/`typecheck`/`test`/`build` green locally and as CI checks on PRs; a Playwright screenshot script captures any route to a file. |
| E-2 | done | Capture — sessions in | Record from the mic with live level meter and elapsed timer (chunked MediaRecorder — long takes survive); upload mp3/wav/m4a/webm/ogg/aac/flac up to 24 h / 2 GB, streamed to disk (never buffered in memory); duration probed via ffprobe, over-cap files rejected with a truthful message; sessions list + detail with player; ingest-job rows with visible progress states; delete removes rows and files. |
| E-3 | done | Smart ingest — only the speech | Every capture runs an async job: normalize to 16 kHz mono; voice-activity detection extracts speech segments with original timestamps and discards silence/noise; segments under 2 s dropped; each segment content-hashed for dedup and caching; session page shows raw vs speech time ("6 h dump → 47 min speech") and a segment timeline; time-compressed triage renditions (configurable 1.25–1.5×) produced per segment; a day-long fixture processes without exhausting memory and a killed job resumes where it stopped. |
| E-4 | done | Analysis — the cascade coach | Analyze runs the cascade: `gpt-audio-mini` triages compressed segments and flags suspicious windows; `gpt-audio-1.5` deep-listens only the flagged windows at native speed; findings persisted (quote, correction, category grammar/vocabulary/phrasing/idiom/pronunciation, explanation, severity, timestamp) focused on the dominant speaker; a cost estimate from a rates table is shown before the run and the monthly budget cap halts runs truthfully; progress visible without reload; report UI with per-category counts, expandable findings, jump-to-audio at the finding's timestamp; cached segments are never re-billed; parsing covered by fixture tests plus one real-API smoke run documented in the PR. |
| E-5 | done | Flashcards — drill your own mistakes | Every finding generates a card (front: your phrase in context; back: correction + why), deduplicated; Practice shows the due queue; full-screen practice with 3D flip and Again/Hard/Good/Easy grading persisted through SM-2 scheduling; keyboard shortcuts (space to flip, 1–4 to grade); card browser with suspend and delete; export to CSV importable by Anki. **End of v0.1.** |
| E-6 | done | Micro-lessons | Recurring error patterns become short grammar lessons with interactive exercises (multiple choice, fill-in, rewrite graded by the model with feedback); completing a lesson updates pattern mastery. |
| E-7 | done | Focus map & progress metrics | Error rate per speaking-hour by category, trended across sessions; ranked "what to work on next"; the whole answer on one screen. |
| E-8 | folded | Pronunciation studio → E-37 | Re-record a corrected phrase; the audio model compares your take against the target and gives specific feedback; minimal-pair drills generated from your pronunciation findings. Subsumed by E-37 (hybrid scoring per D-21). |
| E-9 | done | Phrasebook — the recast library | A searchable bank of "you say X, natives say Y" built from findings, including full-utterance recasts side by side; pin any entry into flashcards. |
| E-10 | folded | Conversation gym → E-34 | Realtime voice roleplay (`gpt-realtime`) that steers scenarios into your weak patterns; a gym session yields findings like any capture. Subsumed by E-34 (the tutor). |
| E-11 | done | Speech archive | A searchable timeline of your analyzed speech (transcripts exist only as analysis byproducts), filterable by category and severity — your speaking life at a glance. |
| E-12 | done | The editor's letter | A weekly digest: trends, best recasts, the one thing to focus on next week. Quiet ritual, no gamification. |
| E-13 | folded | Voice enrollment — only you → E-36 | A 30-second enrollment teaches Erika your voice; day dumps are filtered to your speech before analysis — bystanders are never analyzed (privacy) and never billed (cost). Subsumed by E-36 (local, recall-first per D-22; the take is captured at E-35). |
| E-14 | folded | Hosted + OAuth → E-40 | The same app deployed multi-user behind OAuth sign-in. Subsumed by E-40. |
| E-15 | folded | Companion & mobile → E-41 | An authenticated ingestion API an always-on recorder pushes to; an Expo app sharing the same routes — Erika without a browser. Subsumed by E-41 (native SwiftUI over the same API per D-17/D-25). |
| E-16 | done | Hardening & integrity | Cache-reused findings carry timestamps remapped onto the target session's segment (regression test with an offset-shifted duplicate hash); job claim uses a heartbeat lease so a second worker cannot re-run a live job, plus an insert-if-absent guard on `findings`; VAD splits any speech interval over `MAX_SEGMENT_MS` at its quietest contained dip so no segment is buffered whole; spend is recorded when a billable call *resolves*, not after parse (a parse failure still ledgers the charge). **Part 2 — the operator's own session:** `npm run worker` loads `.env.local` and exits non-zero without an API key; a job sitting queued with no live worker heartbeat says so in the UI; VAD thresholds on the recording's *measured* noise floor with pre/post-roll padding and a widened merge gap, calibrated against a committed labelled sample (recall asserted, D-13); a truncated or unparseable model reply gets one bounded repair retry and then marks only *that* segment unreadable while the run completes and reports the count; Analyze is refused (409) and not offered on a session with no segments; a lost mic take says so; the polling hooks stop on 404/410; the pre-commit hook blocks raw NUL bytes in source. |
| E-17 | done | One findings truth | A single `lib/findings-model.ts` defines "analyzed session" and "included finding"; all six read sites consume it and agree on the same week's counts (test covering the previously-divergent halted-run and re-analysis cases); focus/letter aggregates computed in SQL rather than per-session loops with identical results; a schema/ER doc lands and is bound to the migration ritual. |
| E-18 | done | The honest home | `/practice` composes a daily plan (due cards + the lesson Focus's ranking prescribes + the unread letter) instead of an interstitial; sessions rows show yield (speech time, findings, dominant category) and unanalyzed sessions are visually distinct with an inline analyze affordance showing its estimate; Settings shows month-to-date spend against the cap; lesson rows state "Lesson ready" vs "Generate — est. $X"; green is reserved for resolved/mastered/improving (LOW and READY badges lose it). |
| E-19 | done | Profile-primed analysis | A compact speaker profile (native language, top recurring quote→correction pairs, category rates, mastery) is built without a model call and injected into triage, deep-listen, and lesson prompts (fixture tests assert the L1 and profile lines); the deep model marks findings that recur a profile entry and the recurrence link is persisted. |
| E-20 | done | Slips — the fossil dossier | Findings cluster into persistent slips with a stable key; each slip's dossier interleaves every occurrence (jump-to-audio) with its drill history (card grades, lesson completions) on one timeline; a slip's state computes as active fossil / in remission / resolved from later analyzed sessions containing no matching occurrence ("Not heard since 13 Jul · 3 sessions clean"); Focus shows a resolved count and green finally means mastery. |
| E-21 | done | Contrastive playback | Each finding's correction is rendered once in the audio model's voice, cached per finding and recorded in the shared spend ledger; a Compare control on card backs and phrasebook rows plays your clip then the native rendering; replays make zero model calls (exactly one ledger row per finding); the budget cap refuses generation truthfully. |
| E-22 | done | The session map | The session timeline plots severity-tinted finding markers on their segments, clicking a segment plays it and selecting a finding highlights its segment; Focus gains a "when you slip" distribution by wall-clock hour from a unit-tested pure function. |
| E-23 | done | Ask Erika | An "ask for more" affordance on any finding returns a persisted deeper note citing at least one other finding from the user's own corpus; re-opening it makes zero model calls; budget-capped and ledgered. **End of v0.3.** |
| E-24 | done | Platform boundary & resumable upload | The D-25 conventions land as `docs/api.md` plus a no-op auth middleware stamping the single-user principal; `GET /api/letter`'s viewed side effect becomes `POST /api/letter/viewed`; uploads move to tus (`@tus/server` catch-all route, `tus-js-client` inside `lib/upload-audio.ts`, streamed POST kept as fallback), a completed upload finalizes into the existing ingest pipeline, and partial uploads expire under a stated GC policy. |
| E-25 | done | Knowledge core — evidence log & FSRS | One migration adds `knowledge_items` (lemma+POS with lazy sense, rule, phone), append-only `evidence` (mode weights 1.0/0.6/0.3, ×0.7 audio discount), and the spill queue, documented in `docs/schema.md`; the morph-it form→lemma lookup table lands here as the canonical-ID validator, so no evidence row ever mints an unvalidated lemma ID; scheduling moves SM-2 → FSRS-6 (`ts-fsrs`) with existing cards state-seeded and every review logged as evidence; findings bridge to evidence only through the E-17 scope, on validated IDs; "known" needs D-19's corroboration; a test proves derived state rebuilds identically from evidence alone. |
| E-26 | done | Lexicon & grammar syllabus | Built as a parallel batch in isolated worktrees, two migrations. **Lexicon (E-26a, v17):** 30,786 morph-it-validated Italian lemmas imported from license-clean frequency sources (FrequencyWords OpenSubtitles-2018 aggregated through E-25's morph-it form→lemma map; CC BY-SA attributed; CC BY-NC resources stay reference-only), frequency-ranked and coarsely band-labelled from rank (never from an NC source), stoplist-cleaned — ~2× the 15k floor, deep into the colto/literary tail. **Syllabus (E-26b, v18):** a 266-rule A1→C2 grammar curriculum (A1 65 · A2 60 · B1 48 · B2 36 · C1 29 · C2 28; a developed 57-rule C1/C2 *italiano colto* tail) authored as versioned JSON with a prerequisite DAG proven acyclic / resolvable / topologically sortable in tests, seeded as `rule:` items. Both seeds are idempotent, set only reference columns, and never clobber derived SRS/evidence state. Operator gate waived 2026-07-23 (async rule-set report, not a merge block). |
| E-27 | done | Parallel cascade & spend reservations | A per-segment concurrency pool (env-capped, ~6) inside one analysis job; the budget check becomes reserve-before-call (pending ledger rows counted by the cap, finalized on resolve, swept at startup); an interval heartbeat while calls are in flight; bounded jittered 429 retry honoring Retry-After; progress counts completions; a 12 h dump lands in ~10–20 min and concurrency atomicity tests prove the cap cannot be overshot — the operator's constant-wall-clock requirement is met before the dial opens spend. |
| E-28 | done | The richness dial | Captures ≤30 min skip triage and are deep-listened 100% at native speed with the enriched prompt (pronunciation suspects, colto register upgrades, disfluencies); enriched output persists — new finding categories via migration or a structured notes channel, chosen in the work order — and deep max-tokens rises so the E-16 truncation repair stays rare; the deep pass also returns correctly-produced lemma+POS, validated against E-25's morph-it table and written as ×0.7-discounted spontaneous-correct evidence, and recording-attested lemmas are excluded from the composer's new-item selection; day dumps keep the cascade with triage loosened per D-20; `rates.ts` is recalibrated to ~$0.03/audio-min all-in against one real run's usage; the default cap rises to match the posture; estimates stay truthful and cached segments still never re-bill. |
| E-29 | done | Correction-forward, error-once | D-18 lands: card fronts and exercise prompts become meaning-first (English gloss / Italian context gap) — an erroneous utterance is never a practice stimulus; the session report presents each finding correction-first with the original shown once, subordinate and marked; the phrasebook flips to correction-forward with tap-to-reveal; Compare and the slips dossier are unchanged. **End of v0.4.** |
| E-30 | done | Two-tab shell | Record and Learn become the only tabs, phone-first; Library (sessions, archive, phrasebook, slips) lives under Record; practice, focus and the letter live under Learn; Settings behind a gear; every existing deep-link path resolves in place (two convenience aliases `/record`→`/`, `/learn`→`/practice` redirect), asserted by a route→tab + redirect test. Folded RETRO-002 P1 (correction-forward on Archive + the Letter — correction headlined, error tap-to-reveal, D-18) and P2 (the richness-dial `notes` render as a calm "Erika also noticed" aside in the expanded finding). DESIGN-faithful glass tab bar, no migration. |
| E-31 | done | The daily composer | `compose(day)` is pure and unit-tested: spill queue → FSRS-due reviews (worst retrievability first) → active slips → unspent findings → new items at the knowledge edge (defaults 10 vocab / 3 rules / 10 pronunciation, settable), interleaved, overflow spilling to tomorrow; Learn home shows TODAY (cards due, lesson — the tutor row arrives with E-34) with the ink goal ring and a factual completion sentence; a local-day goal-completion ledger (migration **v19**; timezone stance explicit in `lib/local-day.ts`) records every completed day from the start, so the streak is retroactively true when E-38 renders it; composition makes zero model calls. First live consumer of the E-25 knowledge core (freq_rank / status / rule DAG). Landed the D-19 corroboration fixes RETRO-002 flagged: recognition-mode positives never corroborate `known`, and a slip only turns green on a positive production/drill event (not mere absence); plus T2 knowledge-yield instrumentation (a dev-only inspector). **End of the composer; E-32 lesson formats next.** |
| E-32 | done | Lesson formats v1 | Grammar micro-lessons (rule explanation + short meaning-first exercises) and vocabulary intro+practice are generated for composer-chosen items, colto-aware (D-23, default colto), budget-capped and cached like E-6 (reserve-before-call; a cache re-open bills ZERO; parse-fail still ledgers; cap hard cross-biller); completing an exercise writes one cued evidence row (polarity from correctness) through the E-25 door on a morph-it-validated/valid-rule id; no lesson ever shows an erroneous form as a stimulus (D-18). Migration **v20** = the `item_lessons` generate-once cache. **[RETRO-002 P4]** a degraded cloze (register upgrade / whole-phrase rewrite) gets an English-gloss front instead of an unanswerable `____` (D-18 permits). Built to fixtures behind the model-client seam (no key in sandbox); the one real-API smoke is an operator-key-gated follow-up. |
| E-33 | done | Voice & canon | The **register dial** (colloquiale→standard→colto→letterario, default colto, D-23) lands in Settings as one shared `lib/register.ts` instruction injected into **analysis recasts** (the deep-pass correction voice), **lesson generation** (E-32's hardcoded colto replaced by the dial), **TTS delivery instructions** (the render engine's voice/style), and a **documented E-34 tutor-persona hook** (`lib/tutor/persona.ts`) with a test that the hook receives the dial — style only, never correctness (a fixture per surface). A **listen-and-shadow** format renders a finding's **correct** correction (never the error, D-18) through the **generalized E-21 biller** (`billedSynthesize` — reserve-before-call/per-phrase cache/ledger, one money path, no fork; migration **v21** `phrase_renders`) and records a shadow take through **normal capture→ingest** (no scoring — Azure/E-37, D-21). A **reading/listening** format serves a modest, committed, **public-domain** Italian canon (Dante/Manzoni/Collodi/Verga/De Amicis/Leopardi, attributed in a NOTICE, D-19) matched to the learner's CEFR **edge**, with a cached/ledgered optional listen. Money invariants reconfirmed (render once, replay bills ZERO, cap refuses truthfully, hard cross-biller); built to fixtures behind the model-client seam (no key in sandbox); the one real-API smoke is an operator-key-gated follow-up. |
| E-34 | done | The tutor | A Learn-tab spoken conversation on the Realtime API (default flagship `gpt-realtime-2.1`; Settings tier switch → `gpt-realtime-2.1-mini`, replacing the dead Model-Tier control [RETRO-002 P5]) over WebRTC with a **server-minted ephemeral client secret** — the real `OPENAI_API_KEY` never reaches the browser (a test proves it, and that the mint route requires the server key); instructions built from the E-19 profile + active slips + today's composer targets + the E-33 register dial (`lib/tutor/persona.ts`); a `log_evidence` function tool writes append-only evidence on morph-it-validated lemma / seeded-rule ids during the call (invalid ids rejected, never minted); the call records client-side and lands as a **normal session → ingest → deep analysis** (E-17 one truth, no second findings channel); the Learn home gains its tutor row; the D-24 dots surface (no avatar/waveform). Money: per-session estimate + **reserve/lease-before-call** on the ONE spend spine (no forked path) — heartbeat extends the lease, finalize-to-actual clamped to the lease, truthful cap refusal (no token minted at the cap), hard cross-biller cap; NO new table (the tutor lease reuses `spend_ledger`, `content_hash='tutor:<id>'`). Built to mock + fixture behind seams (no key + `api.openai.com` blocked in the sandbox); the live WebRTC call is an **operator-gated follow-up** (documented, not faked). Subsumes E-10. **End of v0.5.** |
| E-35 | done | Placement onboarding | A 3–4 min yes/no vocabulary check (real words per frequency band + pseudowords, response-style-corrected scoring, pure and unit-tested) seeds recognition-only evidence — never "known"; an optional 60–90 s speaking sample runs through normal capture→analysis; the same flow records the ~45 s enrollment take stored for E-36. **Shipped:** model-free scoring (`lib/placement/`, false-alarm-corrected, D-13-tested); recognition seeding of recognized words + sub-level grammar (`lib/knowledge/seed-placement.ts`) → `introduced`, never `known` (verified); the RETRO-003 fix proven end-to-end (a B1-placed learner gets no A1 grammar); migration **v22** `enrollment_takes` (on-device, D-22); calm Learn onboarding + Settings re-run. |
| E-36 | done | Only you — speaker attribution | Local speaker verification against the enrollment centroid in a checkpointed `attributing` ingest stage; segment verdict = max cosine over ~4 s windows; a recall-first threshold gates which speech mints **produced-lemma positive evidence** — a bystander is never credited, the user is never dropped (D-22). **Shipped:** a `SpeakerEmbedder` seam (mirrors `AudioModelClient`, injected by the worker) with a deterministic in-sandbox log-mel embedder (the calibratable default) + an operator-gated sherpa-onnx impl behind the same interface (#47-safe asset path, traced into standalone); the `attributing` stage persists `segments.speaker_score`/`is_user` (nullable = unattributed = treated as the user — degrades honestly with no enrollment / filter off / no model); the cascade gates produced positives by the verdict; a **produced-lemma idempotency key** (partial-UNIQUE `INSERT OR IGNORE`, append-only-safe) and a **session-exclusion** flag + API + a calm session-view toggle (both RETRO-002 owed items); migration **v23**; D-13 calibration against a committed **synthetic** two-voice fixture — voices procedurally generated (ffmpeg formant voices), not recorded, though the embedding + τ decision genuinely run in-sandbox (user recall 1.0, other-exclusion ~57%, a naive baseline falsified); kill-switch env; re-enrollment recomputes the reference; bystander audio never leaves the device (D-22). Subsumes E-13. Live sherpa τ re-calibration against real voices is operator-gated. **End of the v0.6 identity sub-block (E-35+E-36) — a D-12 mid-version retro checkpoint runs here.** |
| E-37 | backlog | Pronunciation studio | Deep-pass pronunciation suspects (persisted at E-28) become re-record drills (minimal pairs) scored by Azure Pronunciation Assessment it-IT at phoneme granularity — scripted text only, D-3 stands; Azure spend enters the same ledger under the same cap; drill scores write evidence. Subsumes E-8. |
| E-38 | done | Streak & map | The streak renders from E-31's day ledger with two automatic silent repairs per month ("Day 14 · repaired Tue"); the Learn map strip tints toward green only via resolved-slip semantics; one completion moment per day — the ring closes on spring physics with one factual sentence, which may cite one positive production event ("You used *magari* correctly Tuesday"); the D-24 ban list is enforced in review. **Shipped:** the run is computed from `day_ledger`'s LOCAL day keys by a pure `lib/streak/` module (clean run, one gap, two gaps, a third gap that ends the run, month rollover, DST, idempotent recomputation — all unit-tested); repairs are **automatic and silent**, two per calendar month charged to the month of the missed day, two consecutive misses are never bridged, and when the credits are gone the run simply ends (no prompt, no purchase, no warning, no countdown, no guilt copy); migration **v25** `streak_repairs` persists the spent credits, whose `local_day` PK + `INSERT OR IGNORE` make recomputing on every read unable to double-spend or rewrite history. `currentRun` counts only days actually completed — a repaired day is bridged, never credited (D-19). The map strip reduces the SHARED read-only `computeSlipStandings` (the same standing Focus's green count reduces, so there is one notion of mastery): a category with heavy activity and nothing resolved stays neutral, tested. "Today's thread" cites the composer target the learner genuinely produced today via the first "evidence on local day D" query — spontaneous, own-speech only, with E-36's segment verdict and `exclude_from_evidence` **re-applied at read time** so a later re-attribution or "not me" un-cites it; nothing qualifying ⇒ **no beat at all** (four negative tests). RETRO-003's UTC-binned "when you slip" now bins by the learner's LOCAL hour through a `localHour` helper on the one local-day seam, with the DST question answered in code and covered by tests (the skipped hour is unreachable; the repeated hour is one bucket twice as wide; Σ(buckets) conserved). **End of v0.6** (merged v0.6+v0.7 — "feature-complete"). |
| E-39 | backlog | Debt sweep | The ledgered leftovers land together: ellipsis-gated slip-prefix clustering, the empty-ask-claim startup sweep, ~~the UTC-basis caption~~ (done at E-38), list pagination, `data/cache` + partial-upload eviction, error-envelope unification. **End of v0.8.** |
| E-40 | backlog | Hosted | The auth seam becomes real OAuth; single-node deploy with Litestream WAL replication; system ffmpeg on the host (D-7 revisited); the tus endpoint sits behind auth. Subsumes E-14. **End of v0.9.** |
| E-41 | backlog | Native iOS | A SwiftUI client over the same JSON API (TUSKit uploads, universal links mirroring today's paths); long-form and background capture become real; the web app keeps working. Subsumes E-15. **v1.0.** |
