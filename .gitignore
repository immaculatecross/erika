# dependencies / build
node_modules/
.next/
out/
coverage/
*.tsbuildinfo

# local data: database + audio sessions
data/

# secrets
.env*
!.env.example

# tooling artifacts
playwright-report/
test-results/
.playwright/
artifacts/
next-env.d.ts
*.log
.DS_Store

# Claude Code harness scratch (agent worktrees, session state) — never committed
.claude/
