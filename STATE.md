# State

> Boot sector: first read of every fresh session. Regenerated when things change — keep it one screen.

Erika founded 2026-07-17 via mfactory ideate, and amended the same day in a second sparring round (D-8…D-12): the design is Apple-grade with color, not monochrome; capture is day-scale (24 h dumps, resumable async ingest); cost is handled by architecture (VAD speech extraction → mini-triage → deep-listen cascade, caching, budget cap); the repo is public with autonomy-first branch protection (PRs + linear history required, zero required approvals — the factory merges on the reviewer session's verdict); the roadmap runs E-1…E-15 with **v0.1 = E-1…E-5** (foundation → capture → smart ingest → analysis → flashcards). The repo is still charter + kit only — no code yet. Charter: PRODUCT.md, DESIGN.md (binding), FEATURES.md, DECISIONS.md (D-1…D-12). Kit pinned in `.mfactory/`; hooks armed via `git config core.hooksPath .mfactory/hooks` — re-arm after any fresh clone. Secrets only in `.env.local` (OPENAI_API_KEY, present, untracked); `gpt-audio-1.5` access verified live at founding.

Next: **E-1 Foundation** (FEATURES.md). The dispatcher writes `.mfactory/work-orders/WO-foundation.md` and runs the loop in `.mfactory/playbooks/dispatch.md` — fresh worker session, fresh review session, merge through the gate. Standing dispatcher duty: after E-1's CI lands, add its check contexts to the branch protection. Operator-owed items: rotate the founding OpenAI key after v0.1 (it transited a chat channel); optionally, a GitHub App reviewer identity later for the formal gate (mfactory M-4) — no extra GitHub account is needed.
