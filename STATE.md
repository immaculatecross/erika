# State

> Boot sector: first read of every fresh session. Regenerated when things change — keep it one screen.

Erika founded 2026-07-17 via mfactory ideate (D-1…D-14, extended D-15/D-16). Apple-grade design, strict monochrome-plus-signal palette (accent black/white; green and red only where a state carries meaning — D-14); capture is day-scale (24 h dumps, resumable async ingest); cost is handled by architecture (VAD → triage → deep-listen cascade, content-hash caching, hard budget cap, lease-before-spend); repo public with autonomy-first branch protection (PRs + linear history + zero required approvals — the factory merges on the reviewer session's verdict). Stack: Next.js App Router (TypeScript strict) + Tailwind tokens + better-sqlite3; system `ffmpeg`/`ffprobe`; secrets only in `.env.local`; DB and audio under `data/` (gitignored). Hooks armed via `git config core.hooksPath .mfactory/hooks`. Gates lint/typecheck/test/build + tripwires run in CI on every PR.

**Shipped.** **v0.1** (E-1…E-5): the whole loop — capture (mic + streamed day-dump upload), smart ingest (VAD speech extraction, hashing, caching), the analysis cascade with budget cap, findings report, SM-2 flashcards. **v0.2** (E-7, E-9, E-11, E-6, E-12): the coaching layer — focus map, phrasebook, archive, micro-lessons, the editor's letter. **v0.3** (E-16…E-23, RETRO-001-scoped, D-16 throughput model): integrity and personalization — hardening, one findings truth (`lib/findings-model.ts`), the honest home, profile-primed analysis, slips (green = mastery), contrastive playback, the session map, Ask Erika. 428 tests green at close; migrations v1–v13 documented in `docs/schema.md`. Run reports RUN-001…003, RETRO-001 (mfactory `runs/`). **v0.4** (E-24…E-29, RUN-004, RETRO-002): the Learn substrate — the D-25 platform seam + resumable **tus** uploads (E-24); the **knowledge core** = append-only `evidence` log + derived FSRS-6 `knowledge_items` + morph-it validator (E-25, **v14**); a bounded-concurrency cascade with a hard **reserve-before-call** spend cap (E-27, **v15**); the **richness dial** — short captures 100% deep-listened, enriched notes, produced-lemma positive evidence (E-28, **v16**); a **30,786-lemma** frequency lexicon (E-26a, **v17**) + a **266-rule** A1→C2 grammar syllabus with a validated prereq DAG (E-26b, **v18**); the **correction-forward/error-once** retrofit (E-29). Migrations v14–v18 in `docs/schema.md`; **557 tests** green at close. The **cold-start walkthrough caught a release blocker** (a fresh-DB migration 500'd every route under Next's webpack server bundle) — fixed (#47) before close, validating the D-17 gate on its first watch version.

**The Learn era — ratified 2026-07-23 (D-17…D-25; roadmap PR merged = ratification).** The operator's vision, grounded by four research spikes (`docs/research/spike-1…4`): Erika becomes a **two-tab product** — **Record** (the spine, richest extraction) and **Learn** (a daily course composed from the user's own recorded mistakes first: micro-lessons + a Realtime tutor). Settled: correction-forward/error-once (D-18 — errors are never drilled, shown exactly once); the knowledge model as append-only production evidence with FSRS-6 strength, license-clean lexicon + Profilo-based syllabus (D-19); the spend-for-signal richness dial — short captures 100% deep-listened, ~$0.22/10 min (D-20); pronunciation = LLM flags, Azure PA it-IT scores scripted drills only (D-21); speaker filtering local-only, recall-first, on-device (D-22); register: italiano colto dial (D-23); calm habit layer — streak, ink ring, one factual completion beat, no confetti ever (D-24); API boundary codified + tus resumable uploads (D-25).

**Ladder (D-17, amended by the trial plan review — two fresh lenses caught 1 blocking + 8 evidenced findings before ratification, all priced and applied): v0.4** = E-24 platform boundary & tus · E-25 knowledge core (evidence + FSRS + morph-it validator) · E-26 lexicon & syllabus · E-27 parallel cascade & reservations · E-28 richness dial (+ positive production evidence) · E-29 correction-forward retrofit. **v0.5** = E-30 two-tab shell · E-31 daily composer (+ day ledger) · E-32 lesson formats · E-33 voice & canon · E-34 the tutor. **v0.6** = E-35 placement onboarding · E-36 speaker attribution. **v0.7** = E-37 pronunciation studio · E-38 streak & map. **v0.8** = E-39 debt sweep. **v0.9** = E-40 hosted. **v1.0** = E-41 native iOS. Old E-8/E-10/E-13/E-14/E-15 are `folded` into E-37/E-34/E-36/E-40/E-41.

**v0.4 CLOSED 2026-07-24 (RUN-004, RETRO-002).** All six milestones shipped (see Shipped above), migrations v14–v18, 557 tests green. Close gates run: **RETRO-002** (two fresh lenses — product + technical — grounded, adjudicated under D-12; visible surfaces are Apple-grade, D-14/D-18 hold, no untracked correctness bug); the **cold-start walkthrough caught + drove a fix for a real release blocker** (#47) and is now confirmed load-bearing; RUN-004 synced to mfactory `runs/`. The E-26b grammar rule-set report goes to the operator async (gate waived).

**v0.5 building — E-30…E-34 toward v1.0** (operator extended the mandate to v1.0, no stops — RUN-004). **E-30 done** (#49, Light review): the **two-tab shell** — a phone-first glass bottom nav (Record · Learn), Library (sessions/archive/phrasebook/slips) under Record, practice/focus/letter under Learn, Settings behind a gear; every existing deep link resolves in place (`/record`,`/learn` aliases redirect), route→tab + redirect contract tested; folded RETRO-002 P1 (correction-forward on Archive + the Letter, D-18 — correction headlined, error tap-to-reveal) and P2 (richness-dial `notes` render as "Erika also noticed"). No migration. 579 tests. **E-31 done** (Full review): the **daily composer + day ledger** — a pure, unit-tested `compose(day)` (spill → FSRS-due worst-retrievability-first → active slips → unspent findings → knowledge-edge new items 10/3/10 settable, interleaved, overflow→tomorrow's spill, **zero model calls**), the **first live consumer of the E-25 knowledge core** (freq_rank / status / rule DAG; new-item selection excludes `known` + `recording_attested` and respects prereqs); migration **v19** = the local-day goal-completion ledger (explicit timezone stance in `lib/local-day.ts`, idempotent, authoritative, written from day one); the **Learn TODAY home** with the ink goal ring + one factual completion sentence (D-24); and the folded **RETRO-002 fixes** — recognition positives never corroborate `known`, a slip greens only on a positive production/drill event (not absence), and **T2** knowledge-yield instrumentation (dev inspector at `/dev/knowledge`). **613 tests.** **E-32 done** (Full review): **lesson formats v1** — grammar micro-lessons (rule explanation + meaning-first exercises) and vocabulary intro+practice generated for the **composer's chosen items** (`rule:`/`lemma:` from `compose`), **colto-aware** (D-23, default colto), **budget-capped + cached on the single reserve-before-call biller** — every money invariant reconfirmed: a cache re-open bills ZERO, one ledger row per generation, truthful cap refusal before any call, a parse-fail still ledgers the resolved call, cap hard cross-biller; **completing an exercise writes one cued evidence row** (polarity from correctness) through the E-25 append-only door on a morph-it-validated / valid-rule id, derived state rebuilds; **no stimulus is ever an error form** (D-18); migration **v20** = the `item_lessons` generate-once cache (+ `docs/schema.md`); folded **RETRO-002 P4** — a degraded cloze (register upgrade / whole-phrase rewrite) gets an **English-gloss front**, never an unanswerable `____` (D-18 permits). Built to **fixtures behind the model-client seam** (no `OPENAI_API_KEY` in the sandbox); the one real-API smoke is an **operator-key-gated follow-up** (documented, not faked). New Learn surface: today's grammar/vocab items at `/practice/learn` open into the runner. **636 tests.** **E-33 done** (Full review): **voice & canon + the register dial** — the D-23 **register dial** (colloquiale→standard→colto→letterario, default colto) lands in Settings as one shared `lib/register.ts` instruction injected into **analysis recasts** (deep-pass correction voice), **lesson generation** (E-32's hardcoded colto replaced by the dial), **TTS delivery instructions**, and a **documented E-34 tutor-persona hook** (`lib/tutor/persona.ts`, tested to receive the dial) — style only, never correctness (a fixture per surface); a **listen-and-shadow** format renders a finding's **correct** correction (never the error, D-18) through the **generalized E-21 biller** (`billedSynthesize` — one money path, reserve-before-call/per-phrase cache/ledger, replay bills ZERO, cap hard cross-biller; migration **v21** `phrase_renders`) and records a shadow take through **normal capture→ingest** (no scoring — Azure/E-37, D-21); a **reading/listening** format serves a modest, committed, **public-domain** Italian canon (Dante/Manzoni/Collodi/Verga/De Amicis/Leopardi, attributed in a `lib/canon/NOTICE.md`, D-19) matched to the learner's CEFR **edge**, with a cached/ledgered optional listen. Built to fixtures behind the model-client seam (no key in sandbox); the one real-API TTS smoke is an **operator-key-gated follow-up** (documented, not faked). **672 tests.** **Next: E-34** the tutor (**re-validate the live model landscape — newest OpenAI Realtime — before build; do not trust the training cutoff**). **RETRO-002 folded eight evidenced fixes into these milestones (not new scope); done so far:** finish correction-forward on Archive + Letter → **E-30 ✓**; the slip-green + `known`-corroboration + yield-instrumentation triad → **E-31 ✓**; the gloss-fallback for degraded clozes (P4) → **E-32 ✓**. **Remaining:** surface the richness-dial `notes` (P2, paid-for, still unrendered — was tagged for E-32 but is **not in E-32's WO acceptance criteria, so it was not built; needs re-dispatch**) → **owed**; route pronunciation cards to the studio → **E-37**; remove/repurpose the dead Model-Tier control → **owed / re-dispatch** (was tagged E-33 but is **not** in E-33's WO acceptance criteria, so it was not built — the E-32 richness-notes precedent); O(n²) evidence re-fold → **E-39**. **Operator decisions pending (deferred, D-12):** (1) ratify a "what Erika knows about you" read-only surface as a milestone (P6); (2) the audio cost→real-`usage` reconciliation + the owed rates calibration need a live key (T1) — until then the cap guards the *modeled* budget (hard; the risk is model-cost ≠ invoice, not runaway spend).

**Owed items.** Dispatcher: add the `gates` CI check to branch protection's required status checks; a **CI smoke-boot of the built server hitting a DB route** (the automated closure of the cold-start gate-gap #47 exposed — vitest can't see the webpack realm) → **E-24/E-39**. *(Done at v0.4 close: RUN-004 synced to mfactory `runs/`; E-26b rule-set report delivered async.)* **Deferred/tracked (recorded, RUN-004/RETRO-002):** → E-40, make tus finalize idempotent under a lossy network (E-24 review); → **E-31 ✓ done**, recognition-mode positives excluded from the `known` corroboration count + slip "resolved"/green gated on a positive production/drill event, not mere absence (E-25 review + RETRO-002 P3/T3); → **owed / re-dispatch** (was tagged E-32 but not in E-32's WO acceptance criteria, so unbuilt), render the richness-dial `notes` (RETRO-002 P2 — paid-for signal currently unshown; correction-forward on Archive + Letter landed in **E-30 ✓**); → **E-32 ✓ / E-37**, gloss-fallback for degraded clozes **done (E-32)**; route pronunciation findings to the studio → **E-37** (RETRO-002 P4); → **owed / re-dispatch** (tagged E-33 but not in E-33's WO acceptance criteria, so unbuilt — the E-32 richness-notes precedent), remove/repurpose the dead Model-Tier control (RETRO-002 P5); → **E-31 ✓ done**, knowledge-core yield instrumentation (emitted/attested/dropped counters + a dev inspector) (RETRO-002 T2); → **E-36**, a session-exclusion surface must suppress produced-lemma positive evidence + a produced-lemma idempotency key (E-28 review + RETRO-002); → **E-39** debt sweep, (i) reservation-recovery re-insert buckets under the current month not the original (E-27); (ii) full-deep estimate prices deep at the primary rate — cap-safe (E-28); (iii) ask/render charge→finalize is non-atomic — cap-safe (E-28); (iv) tighten the E-29 front-omit test to D-18's real contract (E-29); (v) fold evidence forward from the FSRS checkpoint to kill the O(n²) re-derive (RETRO-002 T4); (vi) stale `budget.ts` cross-biller comment (RETRO-002); → **E-32/E-33**, polish the `periodo-ipotetico-letterario` example toward idiomatic *colto* (E-26b advisory — content correct). FSRS params are uncalibrated defaults until reviews accrue. **Operator (decisions/resources):** rotate the founding OpenAI key (transited a chat channel) — also unblocks **[RETRO-002 T1]** the audio cost→real-`usage` reconciliation + the owed `rates.ts` calibration (cap today guards the *modeled* budget; risk is model-cost ≠ invoice, not runaway spend); **[RETRO-002 P6]** ratify (or not) a "what Erika knows about you" read-only surface as a milestone; an `AZURE_SPEECH_*` key before E-37; optionally the GitHub App reviewer identity (mfactory M-4).
