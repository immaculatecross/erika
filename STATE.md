# State

> Boot sector: first read of every fresh session. Regenerated when things change — keep it one screen.

Erika founded 2026-07-17 via mfactory ideate. **Master the language you already speak**: a coach for people past fluency, built on their own recorded speech (PRODUCT.md). Apple-grade design, monochrome-plus-signal palette (accent black/white; green and red only where a state carries meaning — D-14). Stack: Next.js App Router (TypeScript strict) + Tailwind tokens + better-sqlite3; system `ffmpeg`/`ffprobe`; secrets only in `.env.local`; DB and audio under `data/` (gitignored). Hooks armed via `git config core.hooksPath .mfactory/hooks`. Gates lint/typecheck/test/build + tripwires run in CI on every PR, and `gates` **is** a required status check on `master` (branch protection: PRs required, linear history, zero required approvals — the factory merges on the reviewer session's verdict, D-11).

**Two processes, always.** `npm run dev` serves the UI and only *enqueues*; `npm run worker` drains ingest and analysis. Without the worker nothing is processed — and since v0.7 the product says so on first paint rather than leaving you to wonder.

**Shipped through v0.6.** **v0.1** the whole loop (capture → smart ingest → the analysis cascade → findings → flashcards). **v0.2** the coaching layer (focus map, phrasebook, archive, micro-lessons, the editor's letter). **v0.3** integrity and personalization (one findings truth in `lib/findings-model.ts`, the honest home, profile-primed analysis, slips where green finally means mastery, contrastive playback, the session map, Ask Erika). **v0.4** the Learn substrate (the D-25 platform seam + tus uploads, the append-only knowledge core with FSRS-6, a 30,786-lemma lexicon and a 266-rule A1→C2 syllabus on a proven-acyclic DAG, the richness dial, correction-forward/error-once). **v0.5** the Learn experience (the two-tab shell, the pure daily composer + local-day ledger, lesson formats, the register dial + public-domain canon, the Realtime tutor). **v0.6 "feature-complete"** (placement onboarding, speaker attribution, the pronunciation studio, streak & map) — which closed as, in its own retro's words, **the best-tested and least usable version of this project**: 1012 tests green while a new user with no API key could not get a single recording through the pipeline.

**v0.7 — the simplification (D-26, E-42…E-46). All five milestones merged.** 1472 tests, migrations through **v30**, 28 decisions. That v0.6 verdict is what this version answers: the gap was never features, so it could not be closed by more of them. **Subtraction was the deliverable** — every milestone named what it deleted. **E-42 capture without buttons** (#71, v28 `captured_at`): record, confirm once, and upload → ingest → **analysis** all run themselves; two Analyze controls, five cost surfaces and the browser's role in starting analysis are gone, and a real capture timestamp replaced the upload instant wherever the app claims *when you spoke* (46 of 189 `created_at` uses reclassified, guarded structurally). **E-43 the tutor** (#74, v29 `tutor_conversations`): listening stays **native audio** and only the voice changed — D-28, after a spike measured `whisper-1` silently *correcting* planted errors, which is D-3 proven rather than cited. Ten voices, a settable 10-minute minimum, cost shown after each conversation, ~740 ms replies. Its `call_id` was discarded, so **corrections were never spoken** on any turn that logged evidence; and **every conversation had been refused `422 undecodable_audio` for two versions** — E-34's own criterion, marked done and false since it shipped. **E-44 one session a day** (#79, v30 `daily_sessions`): one screen, one control, one linear session (lesson → drills → weekly letter → conversation → done); everything else behind one `/library` entry, nothing deleted, 12/12 deep links verified. Its rule is structural — *a step Erika cannot deliver is not in the session at all* — and the lesson, which cannot be dropped, degrades to authored content for all 266 syllabus rules, teaching with no key, no budget and no network. The day is complete when the **session** is; the cards-only goal and its hard-coded `lessonsDone: 0` are gone. **E-45 the lesson and the deck** (#82): one lesson format, click-or-voice only, and **no card front can be unanswerable** (the `____ · category` degradation deleted, totality proved). A mishearing had been writing an **unretractable** negative into the knowledge model; the fix is ordering — nothing is recorded until you continue — and the rule moved out of component state into a pure reducer, because an invariant in React state is verified only by reading it. **E-46 first run and progress** (#85): an empty database **forces** onboarding, which client-side navigation had been walking straight past (an `RSC: 1` request returned 200 and the page); day one is **calibrated** — placed C2, first session at C2; `/progress` replaces the dev-only inspector.

**⚠ v0.7 IS NOT CLOSED. The cold-start gate has not run.** Four attempts were killed by platform failures (529 Overloaded, watchdog stalls) before producing a verdict. It is **non-waivable** — it failed v0.4 (a release blocker) and **failed v0.6 outright, blocking its close**, both times finding what a fully green suite could not. The version stays open until it returns `PASS`, or its `FAIL` is fixed. Also open: `fix/v07-failure-paths` (below).

**The failure-path gate DID run, and found 3 walls and 6 lies.** Worst: **the tutor reported a permanently revoked API key as *"could not reach the conversation service just now"*** — no link, no control. That is the v0.6 defect unchanged, on the milestone v0.7 built to replace it. The diagnosis is the useful part: E-44's `lib/session/notices.ts` discipline held under every condition driven, and **stopped at the session boundary** — every defect sat on a surface it never reached. `fix/v07-failure-paths` extends that one classifier outward (tutor revoked-key + cap, keyless voice controls, Settings' self-contradiction, the failed-ingest sentence); four Library-surface items were **cut by operator ruling** as polish the daily flow never routes to.

**Owed → v0.8.** **1 · Make driving cheap, maintained and standard** — the run's highest-leverage item. Every agent rebuilt the apparatus by hand (10+ times, ~15 min each, uneven proofs) while **12 Playwright specs sit in `e2e/` that CI has never run**. One committed harness that builds, binds a random port, **proves the responder in both directions** and seeds a throwaway DB — plus e2e in CI. When driving is cheaper than writing a test the incentive flips with no new process. **2 · The speaker predicate** (deferred by operator ruling): a bystander's mistakes still become the learner's cards. **3 · Cost optimisation as its own mission** (operator-scoped): a daily flagship conversation models ~100% of the $50 cap; the cost *shown* is the model (~1.95× over) not the invoice, though the browser already receives the `usage` that would fix it; `log_evidence` costs one extra billed response per logged turn. **4 · Close sweep**: the completion beat renders two sentences where D-24 says one; the ingest requeue control; two surviving mutants (`cachedInputRate` `max`→`min`; `FRESH_TEXT_TOKENS_PER_MINUTE` 600→1); the profile disjunction omitting `day_ledger`/`daily_sessions`; the E-44 goal-section assertion now exempting rather than bounding; four cut Library-copy items. **5 · Operator-gated**: rotate the founding OpenAI key; **real human accented speech through the voice-drill path — the one thing v0.7 proved only with synthesised audio**, and the boundary agents cannot cross (`getUserMedia` will not run headless). **6 · v0.8 scope**: E-39 leftovers + **E-40 hosted** + **E-41 native iOS** (this Mac: Xcode 16.2 / Swift 6.0.3 / iOS 18.2 SDK ✅; **simulator runtime not installed**, **0 signing identities**, no Apple account — hosting is the real prerequisite).

**What v0.7 taught, in one line each (full record: `.mfactory/runs/RUN-007-simplification.md`).** *The most valuable finding class is a feature that never worked at all, and only using the product finds it* — a criterion asserting an end-to-end outcome must be verified end to end by observation, never by a unit test of a helper. *Tests are conservation, not discovery* — 18 findings came from contact with the real system, 4 from the suite, and all four were defects in the tests. *Read to form the hypothesis, measure to settle it* — live measurement corrected a reviewer's own prescription that would have under-booked the cap. *State intent and grant authority; do not enumerate criteria* — every good override this version beat the criterion it broke. *The operator's own use is the highest-signal input in the system* — two of the largest course corrections came from minutes of real use, including one that caught a D-26 flaw before a line was written. Total live API spend for the version: **≈ $2.60**.
