# State

> Boot sector: first read of every fresh session. Regenerated when things change — keep it one screen.

Erika founded 2026-07-17 via mfactory ideate (D-1…D-14, extended D-15/D-16). Apple-grade design, strict monochrome-plus-signal palette (accent black/white; green and red only where a state carries meaning — D-14); capture is day-scale (24 h dumps, resumable async ingest); cost is handled by architecture (VAD → triage → deep-listen cascade, content-hash caching, hard budget cap, lease-before-spend); repo public with autonomy-first branch protection (PRs + linear history + zero required approvals — the factory merges on the reviewer session's verdict). Stack: Next.js App Router (TypeScript strict) + Tailwind tokens + better-sqlite3; system `ffmpeg`/`ffprobe`; secrets only in `.env.local`; DB and audio under `data/` (gitignored). Hooks armed via `git config core.hooksPath .mfactory/hooks`. Gates lint/typecheck/test/build + tripwires run in CI on every PR.

**Shipped.** **v0.1** (E-1…E-5): the whole loop — capture (mic + streamed day-dump upload), smart ingest (VAD speech extraction, hashing, caching), the analysis cascade with budget cap, findings report, SM-2 flashcards. **v0.2** (E-7, E-9, E-11, E-6, E-12): the coaching layer — focus map, phrasebook, archive, micro-lessons, the editor's letter. **v0.3** (E-16…E-23, RETRO-001-scoped, D-16 throughput model): integrity and personalization — hardening, one findings truth (`lib/findings-model.ts`), the honest home, profile-primed analysis, slips (green = mastery), contrastive playback, the session map, Ask Erika. 428 tests green at close; migrations v1–v13 documented in `docs/schema.md`. Run reports RUN-001…003, RETRO-001 (mfactory `runs/`). **v0.4** (E-24…E-29, RUN-004, RETRO-002): the Learn substrate — the D-25 platform seam + resumable **tus** uploads (E-24); the **knowledge core** = append-only `evidence` log + derived FSRS-6 `knowledge_items` + morph-it validator (E-25, **v14**); a bounded-concurrency cascade with a hard **reserve-before-call** spend cap (E-27, **v15**); the **richness dial** — short captures 100% deep-listened, enriched notes, produced-lemma positive evidence (E-28, **v16**); a **30,786-lemma** frequency lexicon (E-26a, **v17**) + a **266-rule** A1→C2 grammar syllabus with a validated prereq DAG (E-26b, **v18**); the **correction-forward/error-once** retrofit (E-29). Migrations v14–v18 in `docs/schema.md`; **557 tests** green at close. The **cold-start walkthrough caught a release blocker** (a fresh-DB migration 500'd every route under Next's webpack server bundle) — fixed (#47) before close, validating the D-17 gate on its first watch version. **v0.5** (E-30…E-34, RUN-005, RETRO-003): the Learn experience — the phone-first **two-tab shell** (Record · Learn) with deep-link redirects (E-30); the pure **daily composer** + local-day goal ledger + Learn TODAY home & ink goal ring, the knowledge core's first live consumer (E-31, **v19**); **lesson formats v1** — colto grammar + vocabulary lessons on the cached reserve-before-call biller, exercise→cued-evidence (E-32, **v20**); **voice & canon** — the D-23 register dial injected across analysis/lessons/TTS + listen-and-shadow + a public-domain reading canon (E-33, **v21**); the **Realtime tutor** on `gpt-realtime-2.1` over WebRTC with a server-minted ephemeral secret (the real key never reaches the browser), a `log_evidence` tool, and lease-before-call spend (E-34, no migration). Migrations v19–v21; **719 tests** at close. The model-heavy paths (lessons/TTS/Realtime) are fixture-tested — a live key + `api.openai.com` allowlist is the operator-gated follow-up.

**The Learn era — ratified 2026-07-23 (D-17…D-25; roadmap PR merged = ratification).** The operator's vision, grounded by four research spikes (`docs/research/spike-1…4`): Erika becomes a **two-tab product** — **Record** (the spine, richest extraction) and **Learn** (a daily course composed from the user's own recorded mistakes first: micro-lessons + a Realtime tutor). Settled: correction-forward/error-once (D-18 — errors are never drilled, shown exactly once); the knowledge model as append-only production evidence with FSRS-6 strength, license-clean lexicon + Profilo-based syllabus (D-19); the spend-for-signal richness dial — short captures 100% deep-listened, ~$0.22/10 min (D-20); pronunciation = LLM flags, Azure PA it-IT scores scripted drills only (D-21); speaker filtering local-only, recall-first, on-device (D-22); register: italiano colto dial (D-23); calm habit layer — streak, ink ring, one factual completion beat, no confetti ever (D-24); API boundary codified + tus resumable uploads (D-25).

**Ladder (D-17, amended by the trial plan review — two fresh lenses caught 1 blocking + 8 evidenced findings before ratification, all priced and applied): v0.4** = E-24 platform boundary & tus · E-25 knowledge core (evidence + FSRS + morph-it validator) · E-26 lexicon & syllabus · E-27 parallel cascade & reservations · E-28 richness dial (+ positive production evidence) · E-29 correction-forward retrofit. **v0.5** = E-30 two-tab shell · E-31 daily composer (+ day ledger) · E-32 lesson formats · E-33 voice & canon · E-34 the tutor. **v0.6** = E-35 placement onboarding · E-36 speaker attribution · E-37 pronunciation studio · E-38 streak & map — **merged v0.6+v0.7 into one "feature-complete" version (operator-directed 2026-07-24; RUN-005): after E-38 every learning feature exists.** **v0.7** = E-39 debt sweep. **v0.8** = E-40 hosted. **v1.0** = E-41 native iOS (v0.9 intentionally skipped). Old E-8/E-10/E-13/E-14/E-15 are `folded` into E-37/E-34/E-36/E-40/E-41.

**v0.4 CLOSED 2026-07-24 (RUN-004, RETRO-002).** All six milestones shipped (see Shipped above), migrations v14–v18, 557 tests green. Close gates run: **RETRO-002** (two fresh lenses — product + technical — grounded, adjudicated under D-12; visible surfaces are Apple-grade, D-14/D-18 hold, no untracked correctness bug); the **cold-start walkthrough caught + drove a fix for a real release blocker** (#47) and is now confirmed load-bearing; RUN-004 synced to mfactory `runs/`. The E-26b grammar rule-set report goes to the operator async (gate waived).

**v0.5 CLOSED 2026-07-24 (RUN-005, RETRO-003).** All five milestones shipped (E-30…E-34; migrations v19–v21; 719 tests — see Shipped). Each got a fresh independent review (E-30 Light, the rest Full). Close gates: **cold-start PASS** (all 21 migrations apply cleanly in the bundled server on a fresh DB); **RETRO-003** two-lens retro (D-12) — the design constitution **HOLDS** across the new Learn surfaces (D-18/D-24/D-23/D-19, verified by the product lens), and a dispatcher-approved **defect-fix batch (#54)** fixed three money/correctness issues the lenses caught: item-lesson **lease-before-call** (was unrecorded double-charge under concurrency), **tutor spend recorded-on-crash + a server duration ceiling** (was released-to-$0), and the **slip-green temporal fix** (green now requires a real drill grade *after* the recurrence) — plus the collapsing error-rate metric (min-denominator floor), sub-cent estimate formatting, the inert pronunciation knob, and a flaky gate. *(The slip-green fix's first attempt made green unreachable — evidence rows with no producer — and was caught by re-review before merge: a D-13/D-14 near-miss, logged.)* **v0.6 IN PROGRESS = E-35…E-38** (merged v0.6+v0.7, operator-directed — "feature-complete": placement, speaker attribution, pronunciation studio, streak & map). **E-35 placement onboarding SHIPPED 2026-07-24** (migration **v22** `enrollment_takes`; **736 tests**): a model-free yes/no vocabulary check (real words per frequency band + original license-clean pseudowords, false-alarm-corrected pure scoring in `lib/placement/`, D-13-tested — a pure-guesser scores ~0, not advanced) seeds **recognition-only** evidence (`lib/knowledge/seed-placement.ts`) for recognized words + sub-level grammar → items reach `introduced`, **never `known`** (D-19, verified); the composer now starts near the learner's edge — the **RETRO-003 fix proven end-to-end** (a B1-placed learner is handed no A1 grammar lesson); an optional speaking sample runs the normal capture→analysis path (E-17); a ~45 s enrollment take is stored **on-device only** (D-22) for E-36; calm Learn first-run + Settings re-run (Full review). **Next: E-36.** A **D-12 mid-version retro checkpoint** runs after the E-35/E-36 identity sub-block. E-36 (sherpa-onnx local ML) and E-37 (Azure key + egress) are operator-gated for live validation. **Before E-34-live and E-37: re-validate the model/API landscape with live web research (operator directive) — the E-34 build already did (`gpt-realtime-2.1` family, `/v1/realtime/client_secrets`, audio-token pricing).**

**Owed items.** Dispatcher: add the `gates` CI check to branch protection's required status checks; a **CI smoke-boot of the built server hitting a DB route** (the automated closure of the cold-start gate-gap #47 exposed — vitest can't see the webpack realm) → **E-24/E-39**. *(Done at v0.4 close: RUN-004 synced to mfactory `runs/`; E-26b rule-set report delivered async.)* **Deferred/tracked (recorded, RUN-004/RETRO-002):** → E-40, make tus finalize idempotent under a lossy network (E-24 review); → **E-31 ✓ done**, recognition-mode positives excluded from the `known` corroboration count + slip "resolved"/green gated on a positive production/drill event, not mere absence (E-25 review + RETRO-002 P3/T3); → **E-30 ✓ done** (RETRO-002 P2): the richness-dial `notes` render as "Erika also noticed" (confirmed by the RETRO-003 product lens — the earlier "owed/unrendered" note was stale) alongside correction-forward on Archive + Letter; → **E-32 ✓ / E-37**, gloss-fallback for degraded clozes **done (E-32)**; route pronunciation findings to the studio → **E-37** (RETRO-002 P4); → **E-34 ✓ done**, removed the dead Model-Tier control (RETRO-002 P5 — the real tier switch is now the Realtime flagship/mini toggle); → **E-31 ✓ done**, knowledge-core yield instrumentation (emitted/attested/dropped counters + a dev inspector) (RETRO-002 T2); → **E-36**, a session-exclusion surface must suppress produced-lemma positive evidence + a produced-lemma idempotency key (E-28 review + RETRO-002); → **E-39** debt sweep, (i) reservation-recovery re-insert buckets under the current month not the original (E-27); (ii) full-deep estimate prices deep at the primary rate — cap-safe (E-28); (iii) ask/render charge→finalize is non-atomic — cap-safe (E-28); (iv) tighten the E-29 front-omit test to D-18's real contract (E-29); (v) fold evidence forward from the FSRS checkpoint to kill the O(n²) re-derive (RETRO-002 T4); (vi) stale `budget.ts` cross-biller comment (RETRO-002); → **E-32/E-33**, polish the `periodo-ipotetico-letterario` example toward idiomatic *colto* (E-26b advisory — content correct). FSRS params are uncalibrated defaults until reviews accrue. **RETRO-003 (v0.5) tracked:** → **E-35 ✓ done** (2026-07-24), placement/level seeding fixes the composer's day-1 new grammar being A1 for an intermediate learner — recognition seeding marks sub-level grammar `introduced` so a placed learner is no longer handed an A1 alphabet lesson (proven end-to-end); → **E-37**, route pronunciation findings to a listen-&-compare (shadow engine) instead of an unscorable text cloze; → **E-38**, a "today's thread" beat citing the specific composer target the user just produced; → **E-39**, duplicate TTS billing across `renditions`(finding_id) vs `phrase_renders`(text-hash) [unify → a shared content-hash `tts_renders` cache, N2]; composer review ordering uses `seedStability(interval_days)` not stored `srs_stability`; transient-404 on an in-flight render; focus "when you slip" UTC-binned vs local (D-24); unanalyzed "Analyze" shows no estimate; category-stat crowding at phone width; the stale `cards.ts` `gradeCard` E-28 docstring; → **live-tutor hardening / E-40**, a misbehaving non-heartbeating WebRTC client can incur real OpenAI charge above the recorded ledger (internal cap stays hard; single-user makes the "hostile client" the user) — the true fix is **usage-token reconciliation (ledger = invoice; = T1)**. **Operator (decisions/resources):** rotate the founding OpenAI key (transited a chat channel) — also unblocks **[RETRO-002 T1]** the audio cost→real-`usage` reconciliation + the owed `rates.ts` calibration (cap today guards the *modeled* budget; risk is model-cost ≠ invoice, not runaway spend); **[RETRO-002 P6]** ratify (or not) a "what Erika knows about you" read-only surface as a milestone; an `AZURE_SPEECH_*` key before E-37; optionally the GitHub App reviewer identity (mfactory M-4).
