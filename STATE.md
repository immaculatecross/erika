# State

> Boot sector: first read of every fresh session. Regenerated when things change — keep it one screen.

Erika founded 2026-07-17 via mfactory ideate, amended the same day (D-8…D-14) and extended 2026-07-18 (D-15). The design is Apple-grade with a strict monochrome-plus-signal palette — accent is black (light) / white (dark), green and red only where a state carries meaning, no indigo (D-14); capture is day-scale (24 h dumps, resumable async ingest); cost is handled by architecture (VAD speech extraction → mini-triage → deep-listen cascade, caching, hard budget cap); the repo is public with autonomy-first branch protection (PRs + linear history + zero required approvals — the factory merges on the reviewer session's verdict). Stack: Next.js App Router (TypeScript strict) + Tailwind design tokens + better-sqlite3; system `ffmpeg`/`ffprobe`; secrets only in `.env.local`. DB and audio live under `data/` (gitignored). Hooks armed via `git config core.hooksPath .mfactory/hooks` — re-arm after a fresh clone. Gates `lint`/`typecheck`/`test`/`build` + tripwire scan run in CI on every PR to master (`.github/workflows/ci.yml`, installs ffmpeg); `npm run test:e2e` drives Playwright on a throwaway DB.

**v0.1 COMPLETE — the whole loop ships (E-1…E-5).** Speak → see your mistakes → drill them → take them with you:

- **E-1 Foundation.** The running shell: App Router + Tailwind tokens (light/dark via `prefers-color-scheme`) + a `lib/db` SQLite singleton with an idempotent migrations runner (`_migrations`). Sidebar nav (Sessions · Practice · Settings), DESIGN-compliant empty states, Motion route crossfades + list-stagger (reduced-motion → opacity only). Settings persists target/native language, model tier, monthly budget. `npm run screenshot -- <route>` writes a PNG to `artifacts/`.
- **E-2 Capture.** Upload (streamed to disk under `data/sessions/<id>/`, ffprobe duration, 24 h/2 GB cap) and live mic recording (chunked MediaRecorder, breathing level meter, client-side WAV encode) both land a session with a `queued` ingest job. Sessions list + detail with a Range-served player and delete. Migration v2.
- **E-3 Smart ingest.** `npm run worker` drains jobs: normalize → energy VAD (ffmpeg silencedetect) → content-hashed speech segments (original timestamps, sub-2 s dropped) → cached time-compressed triage renditions. Every audio op is file→file (memory-safe on day-scale input); jobs checkpoint and resume. Read-only session UI polls to a raw-vs-speech summary + segment timeline. Migration v3.
- **E-4 Analysis.** **Analyze** shows a pre-run cost estimate + remaining budget, then runs the cascade (`gpt-audio-mini` triage → `gpt-audio-1.5` deep-listen only flagged windows); findings persisted by `content_hash` (cached audio never re-billed), a hard monthly budget cap halts truthfully, spend recorded atomically with its witness. Report UI: progress orb, per-category counts, findings expanding in place, jump-to-audio. Migrations v4. OpenAI client isolated + mocked in every test (no CI network).
- **E-5 Flashcards.** Every finding → one deduped card (SM-2 scheduler in `lib/srs.ts`, data layer `lib/cards.ts`). Practice shows the due count and a full-screen keyboard drill (`components/flashcard.tsx` 3D flip; space flips, 1–4 grade; reduced-motion → crossfade). **Card browser** `/practice/cards` lists all cards (front/back/category/due/suspended, tabular), **suspends/unsuspends** (a suspended card leaves the due queue), and **deletes** behind a confirm — a deleted card is tombstoned (migration v6 `deleted_findings`) so it never resurrects on regenerate. **Anki CSV export** (`GET /api/cards/export`, RFC 4180-escaped, pure `lib/cards-csv.ts`). Migrations v5–v6.

**Next: v0.2 — the coaching layer over v0.1's findings (D-15), dispatched E-7 → E-9 → E-11 → E-6 → E-12.** Up now: **E-7 Focus map & progress metrics** — error rate per speaking-hour by category, trended across sessions, ranked "what to work on next", the whole answer on one screen. Then E-9 (phrasebook / recast library), E-11 (speech archive), E-6 (micro-lessons), E-12 (editor's letter). Deferred to v0.3 (D-15): E-8 (pronunciation studio, top stretch item), E-10, E-13, E-14, E-15.

**Standing order (D-13 + D-15).** The bare mission "Build the product" runs unattended through v0.1 (done) and on into v0.2 (E-7, E-9, E-11, E-6, E-12, in that order) — one work order at a time through the full loop, merging on the reviewer session's verdict. The operator is interrupted only for a second same-PR rejection, an unanswerable blocker, or a spend/infrastructure halt. The dispatcher writes `.mfactory/work-orders/WO-<slug>.md` and runs `.mfactory/playbooks/dispatch.md` (fresh worker session, fresh review session, merge through the gate). Each version files its own run report (v0.1 = RUN-001; **v0.2 = RUN-002, opened now that E-5 has merged**).

**Owed items.** Dispatcher: add the `gates` CI check context to branch protection's required status checks. Operator: rotate the founding OpenAI key now that v0.1 is done (it transited a chat channel); optionally, a GitHub App reviewer identity for the formal gate later (mfactory M-4) — no extra GitHub account needed.
