# State

> Boot sector: first read of every fresh session. Regenerated when things change — keep it one screen.

Erika founded 2026-07-17 via mfactory ideate, amended the same day (D-8…D-14): the design is Apple-grade with a strict monochrome-plus-signal palette — accent is black (light) / white (dark), green and red only where a state carries meaning, no indigo (D-14); capture is day-scale (24 h dumps, resumable async ingest); cost is handled by architecture (VAD speech extraction → mini-triage → deep-listen cascade, caching, budget cap); the repo is public with autonomy-first branch protection (PRs + linear history required, zero required approvals — the factory merges on the reviewer session's verdict); the roadmap runs E-1…E-15 with **v0.1 = E-1…E-5** (foundation → capture → smart ingest → analysis → flashcards).

**E-1 Foundation shipped.** The running shell exists: Next.js App Router (TypeScript strict) + Tailwind design tokens (light/dark via `prefers-color-scheme`) + better-sqlite3. A persistent sidebar navigates Sessions · Practice · Settings; Sessions and Practice are DESIGN-compliant empty states; Motion drives route crossfades and list-stagger, degrading to opacity-only fades under `prefers-reduced-motion`. Persistence is a `lib/db` singleton at `data/erika.db` with an idempotent migrations runner (`_migrations` table); Settings reads/writes four preferences (target language, native language, model tier, monthly budget) via `app/api/settings` route handlers and they survive a reload. Gates `lint`/`typecheck`/`test`/`build` are green and run in CI (`.github/workflows/ci.yml`) with the tripwire scan on every PR to master; `npm run screenshot -- <route>` writes a PNG to `artifacts/`. Charter: PRODUCT.md, DESIGN.md (binding), FEATURES.md, DECISIONS.md (D-1…D-14). Kit in `.mfactory/`; hooks armed via `git config core.hooksPath .mfactory/hooks` — re-arm after any fresh clone. Secrets only in `.env.local` (OPENAI_API_KEY, untracked).

Next: **E-2 Capture** (FEATURES.md). Standing order (D-13): the bare mission "Build the product" runs v0.1 (E-1…E-5) unattended, merging on the reviewer session's verdict; the operator is interrupted only for a second rejection, an unanswerable blocker, or an unverifiable preflight box. The dispatcher writes `.mfactory/work-orders/WO-<slug>.md` and runs the loop in `.mfactory/playbooks/dispatch.md` — fresh worker session, fresh review session, merge through the gate. Standing dispatcher duty now due: add E-1's CI check context (`gates`) to branch protection's required status checks. Operator-owed items: rotate the founding OpenAI key after v0.1 (it transited a chat channel); optionally, a GitHub App reviewer identity later for the formal gate (mfactory M-4) — no extra GitHub account is needed.
