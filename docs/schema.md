# Database schema

SQLite (better-sqlite3) at `data/erika.db` (`ERIKA_DB_PATH` overrides). Applied by
the runner in `lib/db.ts` from the append-only list in `lib/migrations/index.ts`;
`_migrations` records the versions already applied. **Latest version: v30.**

> **Ritual.** Adding a migration updates this file in the same PR. A schema doc
> that lags the schema is worse than none — it is believed and it is wrong.

## The shape of it

A **session** is one recording. Ingest cuts it into **segments** — the intervals
where someone is speaking — each content-hashed. Analysis reads segments and
writes **findings**, one per mistake. Most findings become a **card**; a
*pronunciation* finding becomes a studio **drill** instead (E-37 — a typed cloze
cannot test a mispronunciation). Findings group into patterns that become **lessons**.

```
sessions ──< ingest_jobs                    (one ingest run per upload)
         ──< analysis_jobs                  (one row per Analyze press)
         ──< segments ──┐
         ──< findings ──┤ content_hash (not an FK — the cache key)
                        └──> segment_analyses
findings ──1:1── cards ──< (SM-2 state)
findings ──1:1── deleted_findings           (tombstone: "do not re-card")
findings ──1:1── finding_slips ──> slips    (one recurring mistake = one slip)
findings ──1:1── renditions                 (render-once contrastive-playback clip)
findings ──1:1── ask_notes                  (ask-once deeper note, cites other findings)
findings ──<  pronunciation_visits          (the no-key loop worked; FK-free — outlives the finding)
findings ──<  pronunciation_attempts        (scored drill takes; nullable, FK-free — outlives the finding)
lessons / lesson_mastery ── pattern_key     (no session FK — derived from all findings)
spend_ledger                                (no FK at all — money outlives sessions)

knowledge_items ──< evidence                (append-only log; derived state is a cache)
                ──< spill_queue             (composer overflow; wired in v0.5)
                ──1:1── item_lessons        (generate-once micro-lesson cache, E-32)
cards ──> knowledge_items                   (nullable item_id, unpopulated: reserved review→evidence link, E-25)
```

`content_hash` is a SHA-256 of the extracted segment audio and the spine of the
cost model (D-10): identical audio is analysed once, ever. It deliberately is not
a foreign key — `segment_analyses` and `spend_ledger` are keyed by hash so they
survive the deletion of any one session that happened to contain that audio.

## Tables

| Table | v | Key | What it holds |
|---|---|---|---|
| `settings` | 1 | `key` | Target/native language, model tier, monthly budget. Values are text. |
| `sessions` | 2, 23, 28 | `id` | One uploaded or recorded file: filename, format, bytes, duration, and TWO timestamps that mean different things. `created_at` is when the ROW was made — i.e. when the upload finished. From v28, **`captured_at` (TEXT, nullable, backfilled to `created_at`) is when the learner SPOKE**, and it is the only column any "when you recorded this" claim may read (E-42): capture is day-scale and async (D-9), so a take recorded at 08:10 and uploaded at 21:30 differs by thirteen hours, and reading `created_at` for it put the recording in the wrong part of the day, the wrong local day, the wrong ISO week and the wrong hour bucket. Resolved once at the finalize gate by `lib/capture-time.ts` (mic take-start → embedded `creation_time` via ffprobe → the file's modification-time hint → the upload instant) and read through `capturedAtSql()`, which coalesces to `created_at` so a NULL can never drop a session out of a histogram. Indexed (`idx_sessions_captured`) because the sessions list is ordered by it. Deleting cascades to jobs, segments, findings. From v23, `exclude_from_evidence` (0/1, DEFAULT 0) — the manual "this recording isn't me" flag (E-36, D-22): an excluded session mints NO produced-lemma positive evidence on its next analysis run, regardless of the acoustic verdict; findings/corrections are unaffected. |
| `ingest_jobs` | 2, 3, 8 | `id` | The speech-extraction run for a session: `state` (queued/processing/done/failed), `stage`, `progress`, `error`, and the v8 lease (`worker_id`, `heartbeat_at`). |
| `segments` | 3, 23 | `id`, unique `(session_id, idx)` | One kept speech interval: original-timeline `start_ms`/`end_ms`/`duration_ms` and its `content_hash`. From v23, the on-device speaker verdict (E-36, D-22): `speaker_score` (REAL, nullable) = max cosine over the segment's 3–5 s windows against the enrolled reference; `is_user` (INTEGER, nullable) 1 = the enrolled user, 0 = another speaker, **NULL = unattributed** (no enrollment, filter off, or a hiccup — treated as the user downstream, so attribution never silences an un-enrolled learner). |
| `findings` | 4, 8, 10, 16 | `id`, unique `(session_id, content_hash, start_ms, quote, correction, category)` | One correction: `quote` → `correction`, `category`, `explanation`, `severity`, timestamps. The v8 identity index makes a replayed write idempotent — it is not a defence against a double run (the lease is). From v10, nullable `recurrence_of`: the CLIPPED (≤60-char, ellipsis-terminated) correction text of the speaker-profile entry the deep model marked this finding as recurring (`lib/analysis/profile.ts` clips it for the prompt, so the stored link is a prefix of the full correction, never equal to it); NULL everywhere the model made no such claim. From v16, nullable `notes`: the E-28 enriched-observation channel — a small JSON object `{pronunciation?, register?, disfluency?}` (a pronunciation suspect, an italiano-colto register upgrade, a disfluency note; each an optional short string), chosen over new categories so the closed `category` vocabulary every surface switches on is untouched; NULL when the model returned no enrichment. |
| `analysis_jobs` | 4, 8 | `id` | One cascade run: `state` (queued/processing/done/failed/**halted**), `stage`, `progress`, `error`, plus the v8 lease. `halted` = the budget cap stopped it. |
| `segment_analyses` | 4, 9 | `content_hash` | The never-re-bill witness: `flagged` (triage's verdict), `deep_done`, and from v9 `unreadable` + `unreadable_reason` + `response_shape` (content-free). **This table is what "analysed" means** — see `lib/findings-model.ts`. |
| `spend_ledger` | 4, 15 | `id` | One row per billable call: `month` ('YYYY-MM'), `model`, `content_hash`, `cost_usd`. No session FK — deleting a session must never erase spend history. From v15, `state` ∈ {pending, committed} (DEFAULT committed — every pre-v15 row is a real finalized charge) + `reserved_at`: the E-27 reserve-before-call machinery so the cap stays hard under the concurrency pool. A call inserts a **pending** row for its estimated cost *before* it fires (`reserveSpend`, atomic: committed + pending + cost ≤ cap or refused), then **finalizes** it to committed on resolve (`finalizeReservation`, in the same txn as findings + witness) or **releases** it on a no-charge failure. The cap counts committed + pending; `monthToDateSpend`/`wouldExceedBudget` (display + the other billers' guard) count committed only. A crash between reserve and finalize leaves a pending row the startup sweep (`sweepStaleReservations`, TTL 15 min) releases. The E-34 realtime tutor reuses this ONE ledger (no new table): a tutor session's lease is `pending` rows keyed by `content_hash = 'tutor:<id>'`, reserved through the same `reserveSpend` (cap stays hard), finalized on end to one committed row clamped to what was reserved (the finalized duration floored at the server-tracked elapsed time so a client under-report can't under-pay, [T2c]), and — if a client stops heart-beating — reclaimed by the same sweep, which **commits** the reserved amount for an abandoned `tutor:` lease (a live session assumed to have run must not vanish from the ledger, [T2a]) while releasing every other stale reservation (`lib/tutor/money.ts`). E-37 adds the FIRST non-OpenAI biller to this same ledger: one Azure pronunciation assessment reserves `pending` under `content_hash = 'pa:<attempt id>'`, is finalized to the cost of the take's ACTUAL audio seconds clamped to what was reserved, and — because the reservation is taken immediately before the request leaves — an abandoned `pa:` lease **commits** on sweep exactly like a `tutor:` one (`isAssumedRunLeaseHash`): a crash with the audio already on the wire must still record the spend. A cap refusal makes no call, so it leaves no row at all (`lib/pronunciation/money.ts`). |
| `cards` | 5, 14 | `id`, unique `finding_id` | The drill card and its scheduler state (`ease`, `interval_days`, `repetitions`, `due`, `last_grade`, `suspended`). From v14 those columns are the FSRS-6 state projected back onto the SM-2 shape (`lib/srs.ts`), plus a nullable `item_id` RESERVED to link a card to the knowledge item its reviews would be evidence for. It is UNPOPULATED in shipped code — no pass attaches a lemma to a card (E-28 records produced lemmas as lemma-keyed `evidence` with `source_ref` NULL, never via `cards.item_id`), so the `if (card.itemId)` review-evidence write in `lib/cards.ts` is dormant until a future pass fills it. Slip green does NOT depend on it: `lib/slip-events.ts` reads the passing drill grade directly off the card (`last_grade` ∈ good/easy, timestamped by the `due - interval_days` grade instant). |
| `deleted_findings` | 6 | `finding_id` | Tombstone so a deliberately deleted card is not regenerated. Pinning from the Phrasebook clears it. |
| `lessons` | 7 | `id`, unique `pattern_key` | One generated lesson per recurring pattern (`category:<category>`, `lib/lessons/patterns.ts`); `exercises` is JSON. Unique = generated once, re-opened free. |
| `lesson_mastery` | 7 | `pattern_key` | Per-pattern mastery 0..1, updated by the EMA rule in `lib/lessons/mastery.ts`. |
| `item_lessons` | 20 | `item_id` (→ `knowledge_items`, cascade) | One generated micro-lesson per composer-chosen knowledge item (E-32, D-18/D-23): a grammar `rule:<key>` or a lemma `lemma:<lemma>#<POS>`. `kind` ∈ {grammar,vocab}; `register` is the D-23 register it was written in (colto by default); `body` is the validated lesson (intro + typed meaning-first exercises, each with its correct answer + rationale, `glossEn` for vocab) as JSON. The `item_id` PK is the cache key AND the one-generation-one-charge guard — re-opening a lesson is a pure cache hit (no model call, no ledger row), the `lessons.pattern_key` precedent. Stores GENERATED content only; a completed exercise writes cued `evidence` through the E-25 door, never here. |
| `slips` | 11 | `id`, unique `slip_key` | One recurring mistake (E-20): `category`, the representative `correction`, and the deterministic `slip_key` (`category:<normalized correction>`). A materialization of the pure clustering in `lib/slips.ts`, upserted by key so re-analysis of the same findings keeps the same id. State (active/remission/resolved) is computed, never stored. |
| `finding_slips` | 11 | `finding_id` | The finding→slip association: one slip per finding, cascade-deleted with its finding. Rewritten idempotently by `materializeSlips`. |
| `renditions` | 12 | `finding_id` | One contrastive-playback rendition per finding (E-21): `path` (mp3 under `data/renditions/`), `cost_usd` (the actual TTS charge, also ledgered once). The `finding_id` PK is both the render-once cache key and the render lease: the engine claims the row BEFORE the budget check and the provider call (lease-before-spend), so exactly one racing Generate calls the model and bills — the loser makes no call and no ledger row (D-15). An uncommitted claim (budget refusal / failed synthesize) is released so a retry can re-lease. FK cascade on delete; the file is cleaned by the delete route and playback is orphan-safe without it. |
| `knowledge_items` | 14, 16, 17, 18 | `id` | One thing the user is learning (E-25, D-19): a lemma+POS (`lemma:<lemma>#<POS>#<sense>`, `sense_key` NULL until a split is forced), a grammar `rule:<key>`, or a `phone:<symbol>`. `kind` ∈ {lemma,rule,phone}; `freq_rank`/`cefr` are the knowledge edge / banding. From v17 those two REFERENCE columns are SEEDED for the lemma inventory: `freq_rank` is a dense unique rank (1 = most frequent) and `cefr` a coarse band DERIVED FROM RANK (A1…C2 — a license-clean frequency proxy, **not** a measured CEFR level; Kelly's real bands are CC BY-NC and out of the path). `prereqs` is a JSON id array (rules only, the prerequisite DAG); from v18 the E-26b grammar syllabus seeds one `rule:<key>` row per rule with its `prereqs`/`cefr` (the DAG-validated A1→C2 curriculum). The `srs_stability`/`srs_difficulty`/`srs_last_event_at`/`status` (∈ unseen/introduced/learning/known/lapsed) columns are a **derived cache**, rebuildable from `evidence` alone and never the source of truth (`lib/knowledge/derive.ts`). From v16, `recording_attested` (0/1, DEFAULT 0) is another derived-cache column: 1 once the evidence log carries a spontaneous, audio-derived, finding-sourced positive row for this lemma (the user produced it correctly in a recording, E-28), so the daily composer (E-31) excludes it from new-item selection. A lemma id can only exist for a (lemma, POS) morph-it attests (`lib/lexicon/morphit.ts`); the v17/v18 seeds set only the reference columns and never clobber derived/evidence-driven state. |
| `evidence` | 14, 23 | `id`, index `(item_id, created_at)`, partial unique `idx_evidence_produced_idem(source_ref)` | **Append-only** (BEFORE UPDATE/DELETE triggers `RAISE(ABORT)`) log of the user's own production (E-25, D-19): `item_id` → `knowledge_items`, `source` ∈ {finding,exercise,tutor,placement}, `source_ref` (TEXT, not an FK — evidence outlives the sessions/findings it cites, the `spend_ledger` precedent), `polarity` 0/1, `mode` ∈ {spontaneous,cued,recognition}, `weight` (the mode weight 1.0/0.6/0.3, ×0.7 when audio-derived), `session_id`. The one source of truth; the `knowledge_items` cache is derived from it and disposable. From v23, `idx_evidence_produced_idem` is a PARTIAL unique index over produced-lemma positives (`source='finding' AND mode='spontaneous' AND polarity=1 AND source_ref IS NOT NULL`) keyed by the idempotency `source_ref` `<session>:<segment content_hash>:<lemma>#<POS>` — a replayed deep-listen re-emits the same key and the writer's `INSERT OR IGNORE` is a no-op (append-only-compatible: a skipped insert, never an UPDATE/DELETE), so no duplicate produced row is appended (E-36). Legacy produced rows carry a NULL `source_ref` and are excluded from the index, so it builds on an existing DB without collapsing history. |
| `spill_queue` | 14 | `id`, index `planned_for` | A knowledge item planned for a day that overflowed today's quota (E-25 creates the table; the daily composer that drains it is v0.5/E-31). Cascade-deleted with its item. |
| `day_ledger` | 19 | `local_day` | One row per LOCAL day the user met their daily goal (E-31, D-24): `local_day` ("YYYY-MM-DD", the local calendar day — timezone stance in `lib/local-day.ts`: a streak day is a local day, never UTC; DB timestamps stay UTC, only this KEY is local), `completed_at` (UTC instant first recorded), `cards_done`/`lessons_done` (the factual figures the one-per-day completion sentence states). The PK makes recording idempotent (INSERT OR IGNORE — a day is never double-counted); the row is AUTHORITATIVE (a completed day cannot be rebuilt from other tables after midnight) and written the moment the goal is first met, so E-38's streak is retroactively true. Figures are written once and never revised. |
| `streak_repairs` | 25 | `local_day` | One row per LOCAL day a streak REPAIR CREDIT was spent on (E-38, D-24): `local_day` ("YYYY-MM-DD", the missed day that was bridged), `charged_month` ("YYYY-MM", the LOCAL month the credit was charged to — stored, not re-derived, so the ledger stays auditable if the fairness rule ever changes), `created_at` (the usual UTC instant). D-24's mechanic: **two automatic, silent repairs per calendar month, earned not bought** — a single missed day inside an otherwise-continuous run is bridged with no prompt, no modal and nothing purchasable; two missed days in a row are never bridged; when a month's credits are gone the run simply ends (no warning, no countdown, no guilt copy). The `local_day` PRIMARY KEY is the whole idempotency story: the streak is DERIVED and recomputes on every read, re-offering the same repair for the same missed day, and the writer's `INSERT OR IGNORE` makes that a no-op — a credit can never be double-spent and history is never silently rewritten. Credits are counted from the whole ledger, so a spent credit stays spent even after the day it bridged falls out of the current run. Deliberately FK-FREE: a repair records a day the goal was NOT met, so there is no `day_ledger` row to reference (the `spend_ledger`/`phrase_renders` precedent), and `day_ledger`'s "a row exists only when the goal was met" semantics are untouched. |
| `phrase_renders` | 21 | `hash` | One rendered CORRECT Italian phrase per (text, register, voice) for the E-33 listen-and-shadow and reading/listening formats: `text`, `register` (D-23), `path` (mp3 under `data/phrase-renders/`), `cost_usd` (the actual TTS charge, ledgered once). The `hash` PK (SHA-256 of voice+register+text, `lib/render/phrase-renders.ts`) is the render-once cache key AND the render lease — the engine claims the row BEFORE the budget check and the provider call (lease-before-spend, the `renditions` pattern), so a concurrent double-render makes at most one billable call (D-15). Deliberately FK-free like `spend_ledger`: a phrase render outlives any one finding/session (a canon passage has none) and its hash-named files are cross-format-shared, so no session delete removes them. All renders go through the ONE E-21 biller (`billedSynthesize`) — no second money path (WO). |
| `speaker_references` | 23 | `(enrollment_id, embedder_id)` | The cached on-device reference embedding for E-36 (D-22): the centroid of an enrollment take's window embeddings under a given embedder. `dim` + `vector` (JSON number array) + `window_count`. Keyed by `(enrollment_id, embedder_id)` so a re-enrollment (a new take id) is a cache MISS and is recomputed, the old rows harmlessly lingering; a different embedder (spectral vs sherpa) also recomputes rather than reusing an incompatible vector. No FK (a reference outlives sessions; the enrollment-take precedent). Derived, rebuildable from the enrollment audio; never uploaded. |
| `enrollment_takes` | 22 | `id` | The ~45 s voice enrollment take(s) captured at placement (E-35, D-22), for E-36's speaker attribution. Metadata only: `path` (the on-disk file under `data/enrollment/`), `format`, `duration_seconds`, `size_bytes`, `created_at`. **On-device only (D-22)** — the audio is never uploaded, hosted, or analyzed; an enrollment take is a voice sample, NOT a session, so it carries no ingest/analysis job and yields no findings. Re-recordable: re-enrollment inserts another row and the newest take (by `created_at`) is the active one. No FK (an enrollment take belongs to the person, not a recording — the `spend_ledger` precedent); `idx_enrollment_takes_created` serves the "latest" read. |
| `tutor_conversations` | 29 | `id`, index `local_day`, index `session_id` | One spoken conversation with the tutor (E-43, D-28) — and the CONTRACT WO-E44 consumes to credit the day. `id` IS the tutor session id, so this row, the spend lease (`tutor:<id>`) and the heartbeat/end routes cannot disagree about which conversation they mean. `started_at`/`ended_at` are the usual UTC text; `ended_at` NULL means live (or abandoned). `duration_seconds` is **server-measured** and a client-reported figure may only ever LOWER it, never raise it — the server's elapsed includes connecting and idling, the client knows how much was really a conversation, and under-crediting yourself harms nobody. (That is the OPPOSITE polarity from the money path, where the server FLOORS the client so a long call cannot be under-paid; each side is the conservative one for its own question.) `min_seconds` is the minimum **in force when this conversation started**, copied in rather than read live, so changing the setting tomorrow never rewrites a recorded day; `met_minimum` (0/1) is decided once at close for the same reason. `session_id` → `sessions` **ON DELETE SET NULL**: deleting the recording must not delete the fact that the conversation happened. It is linked by CAPTURE TIME (E-42's v28 column — the tutor page declares `capturedAt` = the instant the conversation began), and stays NULL when no recording landed. `local_day` is the LOCAL calendar day the conversation closed on (D-24/E-31 — a streak day is a local day, never UTC), stored rather than derived because the reader's timezone need not be the one the learner lived in. Read through `lib/tutor/conversations.ts`, never raw: one reader, one answer to "did a conversation count today". A conversation BELOW the minimum is still real — it logged evidence and its recording still became findings; it simply has not met the bar, and nothing anywhere treats that as a failure (D-24: no countdown, no warning, no guilt copy). |
| `daily_sessions` | 30 | `local_day` | ONE DAY'S SESSION (E-44, D-26): the durable record behind "the day is complete when the session is". `local_day` is the PK, so a day can never fork — opening the session twice is idempotent. `started_at`/`ended_at` are the usual UTC text; `ended_at` fills only when the last step lands. `steps` is a JSON array of the ordered step keys and `lesson_item_id`/`planned_cards` the day's chosen lesson item and card bar — all three **FROZEN AT OPEN and never recomputed**, which is the load-bearing part: every input the planner reads moves while the learner works (answering an exercise writes evidence, grading a card empties the queue, an ingest finishing mints cards), so a session re-planned on each read would grow and shrink under them, the ring's denominator would move, and the home's "what today holds" sentence would be a lie by the time they finished. `done_steps` is a JSON array used as a SET — `markStepDone` is idempotent, so a double-tap, a refresh, a retried POST and a back-button revisit all converge, and there is no counter anywhere to inflate. Steps whose truth is durable elsewhere are VERIFIED server-side and cannot be claimed by the client: the conversation against E-43's `tutor_conversations` (D-26's minimum-duration rule), the letter against the E-24 viewed marker, the drills against `cardsReviewedToday >= planned_cards`. The lesson is deliberately self-reported — reading an explanation leaves no durable trace, and the one trace that exists (an exercise's cued evidence) is absent from exactly the case that matters most, the model-free syllabus lesson. Deliberately NOT evidence and NOT money: it records the shape of a day, and it is rebuilt from scratch every day. No FK — a session references knowledge items and cards that may be deleted, and losing one must not delete the fact that the day happened. |
| `pronunciation_visits` | 26 | `drill_key`, index `finding_id` | One row per pronunciation drill the learner has actually WORKED (E-37): heard the correct line, recorded a take, played it back — **all three**, in that order. The client gates it (`drillGate`, `lib/pronunciation/types.ts`): when the native rendition cannot be played (a budget refusal or a failed render) the learner may still record and hear themselves back, but that lap does NOT write a visit, because the comparison against the rendition IS the drill and a visit retires the correction permanently. No scorer, no score, no money, no upload — `cycles` counts completed loops and `last_at` moves, so repeating a drill is an idempotent upsert rather than a pile of rows. This is the SHIPPED DEFAULT's practice record: the studio's core loop needs no Azure key, so the composer's notion of "this correction has been practised" must not need one either — a pronunciation finding gets no card (a typed cloze cannot test a mispronunciation), so without this it could never be spent and would re-enter the daily plan forever. Deliberately **not evidence**: a visit records activity, and D-19/D-24 are emphatic that green means mastery, never activity — nothing here touches `evidence` or a knowledge item. `finding_id` is nullable and FK-free, like the attempts table: a visit is the learner's own history and outlives the finding that prompted it. |
| `pronunciation_attempts` | 26 | `id`, indexes `(drill_key, created_at)` + `finding_id` | One SCORED take of a scripted pronunciation drill (E-37, D-21). `drill_key` is `<source>:<ref>` — the source-agnostic drill seam, `finding:<id>` today (lib/pronunciation/drills.ts); `finding_id` is the finding it came from, **nullable and deliberately NOT a foreign key** — an attempt is the learner's own history and must outlive the finding/session that prompted it (the `evidence.source_ref` / `spend_ledger` precedent). `reference_text` is the exact line assessed, stored verbatim because it is what the score MEANS. `result` is the whole parsed `PronunciationResult` as JSON (per-word and per-phoneme accuracy, `errorType`, n-best phoneme alternates, and the 100-ns offset/duration ticks that align each word to the learner's own audio), with the four headline scores projected into columns for cheap aggregates. There is **no prosody column**: prosody and syllable groups are `en-US` only, so Italian never has one (OBS-002). `snr_db` (nullable — the service may omit it) + `low_snr` are the re-record gate: a take below the tunable SNR floor is stored, because it was billed, but its scores are never presented as a measurement of the learner (PA quality is bounded by input quality, so a noisy take scores the room). `audio_path` is the take under `data/pronunciation/` (gitignored); a single word slice is a seek into it using the stored ticks. `scorer_id` is provenance — a fixture-sourced score can never be mistaken for a real one — and `cost_usd` the actual charge, also ledgered once as `pa:<attempt id>`. Only SCORED takes appear: a cap refusal writes nothing (no charge and no score), and the optional Azure layer being absent means the studio simply never writes here. |
| `ask_notes` | 13 | `finding_id` | One ask-once deeper note per finding (E-23): `note` text, `cited_ids` (JSON array of the OTHER findings the note cites — ≥1, structurally enforced and resolvable to real included findings), `cost_usd` (the actual text charge, also ledgered once). The `finding_id` PK is both the ask-once cache key and the ask lease: the engine claims the row (with an empty `note`) BEFORE the budget check and the text-model call (lease-before-spend, mirroring `renditions`), so exactly one racing Ask calls the model and bills — the loser makes no call and no ledger row (D-15). The winning call completes the row (fills `note`/`cited_ids`); the cache read returns only completed rows, and every handled failure (budget refusal, a failed/unreadable call) releases the claim so a retry can re-lease. FK cascade on delete — no on-disk file, so the cascade is the whole cleanup. |

Timestamps are SQLite UTC text (`datetime('now')`, `"YYYY-MM-DD HH:MM:SS"`) so
they compare and sort as strings; `lib/jobs/liveness.ts` parses them to epoch ms.

## Reading findings

Do not query `findings` directly from a feature. `lib/findings-model.ts` defines
the scope once — what an analysed segment, an analysed session, and an included
finding are, and how a halted run and an in-flight re-analysis are treated — and
every surface reads through it. Six surfaces once answered that question six ways
and disagreed with each other (E-17). Note that `segment_analyses` witnesses are
hash-shared across sessions (the cache), so the session scopes additionally
require an analysis run of the session's own past `queued`: a byte-identical
re-upload contributes nothing anywhere until its own Analyze runs.

## Migration history

| v | Name | Adds |
|---|---|---|
| 1 | `settings` | `settings` |
| 2 | `sessions_and_ingest_jobs` | `sessions`, `ingest_jobs` |
| 3 | `segments_and_job_checkpoints` | `segments`; ingest checkpoint columns |
| 4 | `findings_analysis_jobs_spend_ledger` | `findings`, `analysis_jobs`, `segment_analyses`, `spend_ledger` |
| 5 | `cards` | `cards` |
| 6 | `deleted_findings_tombstone` | `deleted_findings` |
| 7 | `lessons_and_mastery` | `lessons`, `lesson_mastery` |
| 8 | `job_lease_and_findings_identity` | lease columns on both job tables; `idx_findings_identity` (dedupes existing rows first) |
| 9 | `segment_unreadable` | `unreadable`, `unreadable_reason`, `response_shape` on `segment_analyses` |
| 10 | `findings_recurrence` | nullable `recurrence_of` on `findings` — the profile-entry recurrence link, storing the entry's CLIPPED (≤60-char) correction, not the full one (E-19) |
| 11 | `slips` | `slips`, `finding_slips` — persistent recurring-mistake clusters and the finding→slip association (E-20) |
| 12 | `renditions` | `renditions` — the render-once contrastive-playback cache, one row per finding (E-21) |
| 13 | `ask_notes` | `ask_notes` — the ask-once deeper-note cache, one row per finding, citing ≥1 other finding (E-23) |
| 14 | `knowledge_evidence_spill` | `knowledge_items`, append-only `evidence` (+ its no-UPDATE/no-DELETE triggers), `spill_queue`; nullable `item_id` on `cards` — the D-19 knowledge core (E-25) |
| 15 | `spend_reservations` | `state` (pending/committed, DEFAULT committed) + `reserved_at` on `spend_ledger`, and `idx_spend_ledger_pending` — reserve-before-call so the budget cap stays hard when the cascade runs through a concurrency pool (E-27) |
| 16 | `finding_notes_and_recording_attested` | nullable `notes` (enriched-observation JSON) on `findings`; `recording_attested` (0/1, DEFAULT 0, derived) on `knowledge_items` — the E-28 richness dial's enriched deep output and the recording-produced-lemma mark (D-20, D-19) |
| 17 | `lexicon_frequency_seed` | Seeds `knowledge_items` lemma rows from the committed, reduced, attributed asset `lib/lexicon/frequency-lexicon.tsv.gz` (FrequencyWords OpenSubtitles-2018 CC BY-SA, lemmatized through Morph-it! CC BY-SA and morph-it-validated) — sets ONLY `freq_rank` + `cefr` (coarse frequency band), idempotent, never clobbering derived/evidence-driven state (E-26a, D-19). No new table. |
| 18 | `syllabus_grammar_seed` | seeds `knowledge_items` with the E-26b grammar syllabus — one `rule:<key>` row per rule of the DAG-validated, authored A1→C2 curriculum in `lib/syllabus/grammar-it.json`, setting only the reference columns `prereqs` (DAG edges as item ids) and `cefr`, idempotently and without clobbering derived/evidence state (D-19, D-23). No new table. |
| 19 | `day_completion_ledger` | `day_ledger` — the local-day goal-completion ledger (E-31, D-24): one idempotent row per completed local day, written from day one so E-38's streak is retroactively true. Authoritative (not a rebuildable cache). Timezone stance is explicit in `lib/local-day.ts`. |
| 20 | `item_lessons_cache` | `item_lessons` — the generate-once micro-lesson cache, one row per composer-chosen knowledge item (grammar rule or lemma), keyed by `item_id` so re-opening a lesson never re-generates or re-bills (E-32, D-18/D-23). Stores generated content only; the knowledge core is untouched. |
| 21 | `phrase_renders_cache` | `phrase_renders` — the per-phrase TTS render cache for the E-33 listen-and-shadow and reading/listening formats, keyed by a content hash of (text + register + voice) so a correct phrase renders once, ever, and replays bill zero. The `hash` PK is the render-once cache key AND the render lease (the `renditions` lease-before-spend pattern); all renders go through the shared E-21 biller under the same cap (WO — no second money path). No FK (a phrase render outlives any finding/session, D-23/D-19). |
| 22 | `enrollment_takes` | `enrollment_takes` — the ~45 s voice enrollment take(s) captured at placement (E-35, D-22), stored for E-36's speaker attribution. Metadata rows (`path`/`format`/`duration_seconds`/`size_bytes`/`created_at`) for on-disk audio under `data/enrollment/`; **on-device only** — never uploaded, hosted, or analyzed, and NOT a session (no ingest/analysis job). Re-recordable (latest by `created_at` is active); no FK, `idx_enrollment_takes_created` serves the latest read. |
| 23 | `speaker_attribution` | On-device speaker attribution (E-36, D-22): `segments.speaker_score`/`is_user` (nullable per-segment verdict; NULL = unattributed = treated as the user), `sessions.exclude_from_evidence` (the manual "not me" flag), the `speaker_references` centroid cache, and the partial unique `idx_evidence_produced_idem` that makes produced-lemma positives idempotent under `INSERT OR IGNORE` (append-only-safe). Additive: no shipped migration is edited, and produced-lemma positive evidence is now minted only for the user's own speech while findings/corrections and the never-`known` invariant are untouched. |
| 25 | `streak_repairs` | `streak_repairs` — the spent-repair ledger behind E-38's streak (D-24): one row per local day a repair credit bridged, with the local month it was charged to. The streak itself is derived from `day_ledger` and stores nothing; only the SPENT credits need persisting, and the `local_day` PK + `INSERT OR IGNORE` make recomputation (every read) unable to double-spend or rewrite history. Additive, FK-free, no money path. **Assigned as v25 up front while E-37 was in a parallel batch** (the v18-syllabus precedent); E-38 then merged first, and E-37 renumbered its own migration to v26 so the applied sequence stays monotonic — a renumber that needed the repair recorded in the v26 row below. |
| 26 | `pronunciation_attempts` | `pronunciation_visits` + `pronunciation_attempts` (E-37, D-21). A **visit** is the shipped default's record of the loop — hear the correct line, record, hear yourself back — with no scorer, no score and no money; keyed by `drill_key` so repeating it is an idempotent upsert. An **attempt** is one *scored* take: the drill key (`<source>:<ref>`, source-agnostic), the reference line, the learner's take on disk, the whole parsed per-word/per-phoneme result as JSON, the four headline scores, the SNR re-record verdict, the scorer's id and the actual charge. No prosody column (en-US only). Neither table has an FK on `finding_id` — both outlive the finding that prompted them. Additive; nothing existing is touched. The composer retires a pronunciation finding on **either**, which is what keeps the no-key default from looping it forever. **Numbered v26, not v24**: E-38 merged first with v25, so this was renumbered to keep the applied sequence monotonic. **Correction (RETRO-004 §C1):** this row used to add "(it was never merged or shipped as v24)". Never merged to master — but it WAS applied, on `feat/pronunciation-studio` (5c32ac6, 2bcc810), so databases exist recording version 24 for it, and the renumber shipped with no repair step: v26 then re-created tables that were already present and threw on every boot, permanently. `lib/migrations/reconcile.ts` runs before the migration loop and rewrites that stale ledger row to 26 (completing the v26 end state first, since the earlier v24 variant lacked `pronunciation_visits`). It drops no data and is a no-op on a healthy database. |
| 27 | `placement_runs` | `placement_runs` — one row per completed placement vocabulary check, and the mechanism that makes a placement **recoverable** (RETRO-004 §DE-2). Placement-seeded evidence carries `source_ref = 'placement:<run id>'`, and the evidence READ path (`lib/knowledge/derive.ts` `itemEvidence`, via `VISIBLE_PLACEMENT_EVIDENCE`) counts placement rows only from the LATEST run — so a later placement supersedes an earlier one **without rewriting or removing a single row**: `evidence` stays append-only, the v14 triggers stand, the full history remains auditable, and rules the new run does not claim fall back to `unseen` and leave the daily plan. Before this, a careless run that seeded 238 rules as `introduced` was permanent — re-taking the check honestly still served the old level's grammar, and only deleting the database recovered. Ordering is by `seq` (AUTOINCREMENT) and never `created_at`, which is second-granular and would tie two placements made inside one second — arbitrary is a correctness bug for a supersession rule. `level` is nullable (a run that could not place the learner still supersedes); `calibrated` and `false_alarm_rate` record what the run concluded, so the seeding is auditable after the fact. `id` is deliberately not an FK target from `evidence` (evidence outlives what it cites — the `source_ref`/`spend_ledger` precedent). Additive; recognition still never mints `known` (D-19). |

| 28 | `session_capture_time` | `sessions.captured_at` — a real capture timestamp (E-42, D-26). `sessions.created_at` is the UPLOAD instant, and every surface that claimed to know when the learner SPOKE was reading it: "this morning's recording" (`lib/today-thread.ts`), Focus's local-hour histogram (`lib/slip-hours.ts`), the editor's letter's ISO weeks (`lib/letter.ts`), the Archive's day groups, a slip's first/last occurrence, and the session page's own field literally labelled "Captured". Added NULLABLE because SQLite cannot add a NOT NULL column with a non-constant default, then backfilled to `created_at` — the best value that exists for a recording captured before the column did — so no row is left without an answer. Every read goes through `capturedAtSql()` (one dialect, `lib/capture-time.ts`), which coalesces to `created_at`. Additive: `created_at` keeps its exact old meaning and is still read wherever the question really is "when was this row written" (job age, evidence mint time, the spend month). |
| 29 | `tutor_conversations` | `tutor_conversations` — the durable per-conversation record behind E-43's minimum duration, and the contract WO-E44 reads to decide whether the day is credited (D-26: the day's goal counting flashcards alone is the mechanical root of the "pile of optional errands" feeling). Until now a tutor call left nothing behind but ledger rows and, if the upload worked, an ordinary session — nothing could answer "did a real conversation happen today, and did it last long enough to count". Written at open, closed at end, idempotent on close so a retried `/end` or the `pagehide` beacon racing the button can never rewrite a recorded day. A conversation left open past the point where it could still be running is closed with a NULL duration and `met_minimum = 0`: we do not know how long it ran, and the honest answer to an unknown is not a favourable guess. Additive; no shipped migration is edited. |

| 30 | `daily_sessions` | `daily_sessions` — the durable record of ONE DAY'S SESSION (E-44, D-26), and the mechanism behind two things nothing else in the schema could answer: WHERE the learner left off (so closing the tab and returning resumes at the step they were on) and WHAT today's session is (frozen at open, so the plan cannot shift under them mid-session). It is also what makes criterion 4 possible: the day is recorded complete when every step in `steps` is in `done_steps`, replacing a goal that counted flashcards alone and a hard-coded `lessonsDone: 0`. Recorded history is untouched — no past `day_ledger` row is recomputed, `lib/streak/` reads only `local_day` as it always did, and the new rule therefore applies from the day it ships by construction rather than by a date check. **v29 (`tutor_conversations`) belongs to E-43**, which merged while this branch was open; both are registered and the runner applies them in version order (v29 then v30) — no migration was renumbered, which is the failure mode that left v0.6 databases unbootable. Additive; no shipped migration is edited. |

Never edit a shipped migration — add the next one. `tests/migrations.test.ts`
asserts a fresh database reaches the latest version and that re-running is a no-op.
